# Plugins — Index

Every folder in `/plugins/<name>/` is a self-contained unit (manifest + `index.js`). This is the
fast lookup: **which plugin owns a given verb or mechanic, and how it hooks into the engine.** For the
manifest schema and README convention, see [plugin-standard.md](plugin-standard.md). For the load
mechanism, see `server/engine/plugins.js`.

## ⚠️ Command precedence — plugins win over engine builtins

This is the single most important thing to know before editing any player command. The dispatch order
in `server/engine/commands/index.js` (`handleCommand`) is:

1. SIFT selection-state intercept
2. `fireInputMatchers(…)` — raw-input regex matchers for multi-word verbs (`registerInputMatcher`;
   the MIS plugin registers "jerk off on" / "eat out" through this)
3. **`fireCommand(cmd, …)` — plugin-registered commands** ← runs first
4. `fireSpecializedAction(cmd, …)` — tag-gated specialized actions (also plugins)
5. **`builtins.get(cmd)` — engine handlers** in `server/engine/commands/*.js` ← runs last

So if a plugin registers a verb (e.g. `sit`), the matching handler in `server/engine/commands/*.js`
is **dead code** — it never executes. A verb can have an engine handler *and* a plugin handler; the
plugin always wins. When a command "doesn't behave like the engine code says," check the plugin
registry **first**. (This exact trap caused the posture/HP-regen bug — see
[systems-posture.md](systems-posture.md).)

The loader now makes collisions visible at boot: it **warns** when a plugin verb overrides another
plugin's, and prints an ℹ line listing every plugin verb that shadows an engine builtin. Load order
is filesystem-alphabetical unless a manifest declares `"after": ["otherPlugin", …]` — use it whenever
a verb override is intentional (gametable does this to win `watch` from broadcast + interactions).
A manifest may also declare `"critical": true`: that plugin failing to load aborts boot instead of
logging and limping on (the weapon plugin is critical — without it nobody can fight).

To list what's actually registered at runtime: `getRegisteredCommands()` in `plugins.js`.

**Before deploying any change to verbs, seams, or plugins, run `npm run test:regress`** — it sweeps
every manifest against the live registries and runs each plugin's `regress.js` suite (see
[plugin-standard.md](plugin-standard.md) and CLAUDE.md "Regression testing").

## ⚠️ A tablet button is not a way to do something

**Every operation must have a verb.** A tablet app is a *view over verbs* — the way the Kit screen
sits over `equip`/`drop` and Corporation over `corp …`. When an app's `handleAction` is the only
caller of a function, that function is unreachable by typing, invisible to `help`, and untestable by
`regress.js` (which drives `handleCommand`, not buttons).

An audit on **2026-08-02** found ten operations in exactly that state. Two were whole systems: every
book in the game was reachable only by tapping (`plugins/library` exported no commands at all), and
the entire consort hire-and-place economy was three buttons in the B.L.I.S.S. app. The rest were
`quest track`/`abandon`, the SPECTER destructive ops, aircraft `sell`/`cancelrental`/`flushairborne`
(the last being the *only* recovery for a stranded plane), DEADHEAD remote dispatch, calendar
reminders, remote banking, following a club, and routing to the nearest bench. All ten now have verbs.

Three rules came out of fixing them:

1. **Put the shared logic in the domain module, not the app.** `plugins/library/books.js` and
   `hire.js arrangementEntries()` exist because the app and the verb must not each own a copy — the
   second copy is where the two drift on what something costs or where your bookmark is.
2. **A confirm dialog is a GUI affordance, not a safety mechanism.** Where a button had a modal, the
   verb takes a second command (`quest abandon <name> confirm`, `destruct <name> confirm`). A modal
   is the wrong answer for a typed verb and worse for a screen reader — and with no focused tile, the
   confirmation is also the disambiguation.
3. **Don't shadow an established verb to get a nicer name.** The sports one is `support`, not
   `follow` (interactions owns that), and bench-routing is `findbench`, not `bench` (workspace owns
   that). A shadowed verb is a worse outcome than a longer word.

Check `objectGatedCommands` notes in a manifest before assuming a verb is reachable: `"exposed":
false` means *examine* won't advertise it, which is a milder problem than having no verb at all.

## Plugin catalogue

| Plugin | Owns | Player verbs | Engine surface |
|---|---|---|---|
| **economy-ledger** | Append-only credit audit trail: one `economy_ledger` row per `adjustCredits` mutation (labeled by the caller's `reason`), one `economy_snapshots` row per game day totaling every credit pool. Read only by `tools/economy-report` (circulation, faucet/sink balance, unattributed drift from raw-SQL bypass paths) | — | Consumes `credits.changed` (emitted by `server/engine/economy.js`) + `environment.dayRollover`. Owns `economy_ledger` (`player`) and `economy_snapshots` (`runtime`) in the content registry |
| **interactions** | Posture (sit/stand/lie/kneel), emotes, social actions, furniture interaction, `examine surroundings` | `sit stand lie kneel stretch wave shrug point smile frown laugh cry sigh nod shake dance pace greet follow reflect examine lean watch` (gametable wins `watch` via `after:` and delegates back for plain rooms) | Sets `player.posture` + `player.sittingOn` via `setLivePlayer`. Engine reacts (HP regen, stand-on-attack/move) — see [systems-posture.md](systems-posture.md). **Poker bridge:** when the room has a poker table, `sit <chair>` / `sit at the poker table` dispatches the `gametable.take_seat` action (using the chair's `seat_idx` flag) instead of posturing, and bare `sit` SIFT-prompts *floor vs the poker table* (replayed via the `interactions.sit_choice` action). No import of gametable — the coupling is purely through the action registry |
| **pacing** | Stamina-gated movement pacing — a walk cadence paces travel so the large map feels large (too-fast steps are **queued**, not rejected — type `n n n e` and get walked along at cadence), and `sprint` spends stamina for a faster burst cadence, dropping you to "winded" below a floor | `sprint` | Registers the `pacing:cadence` move gate. A too-fast step is enqueued (`player._moveQueue`, cap 12) and the gate returns a **silent** block (`{block, silent}` → `cmdMove` returns null, no error line); a self-scheduling drain replays each queued step through `cmdMove` at cadence, pushing the result via `sendToPlayer`. Exempt for `opts.bypassEncumbrance` and drained (`opts._pacingDrain`) steps. Sprint spend + `sta` HUD push + cadence-clock stamp happen on the `zone.entered` event. Transient state: `_lastStepAt`, `_sprinting`, `_winded`, `_moveQueue`, `_moveTimer` (see [systems-world.md](systems-world.md)). Reuses the existing stamina resource — no new schema |
| **crafting** | Recipe display & item crafting | `craft recipes` | — |
| **tablet** | Architect Tablet OS — the shared CRT-styled shell (Quests, Skills & Stats, Bank deposit, Weather, Vehicles, Properties, Settings, Corporation). Job Board (`gigs`/`postings`/`jobboard`) and Hangar Pilot Contracts launch straight into Tablet → Quests instead of their own text UI. Corporation renders natively (reshapes `plugins/corps`' own `buildConsolePayload()`/`corp` commands — no logic duplicated, no more hand-off to the standalone corp-console.js overlay). **CODEX** (`codex-app.js` + `codex/`) is the reference shelf: the lore volumes plus the former standalone Ideology app, which is now its `orders` section, payload-unchanged — registers the `CODEX_UNLOCK` action for VINE-authored chapter unlocks; see [systems-codex.md](systems-codex.md). **Vitals** (`health-app.js`, verbs `vitals`/`health`, optional tab argument) is the medical suite: the meters (HP/sanity/stamina/hunger/thirst/radiation/core temp/fatigue/intoxication), everything currently dragging on you (status effects, `conditionReport` stat penalties, live phased drugs, withdrawal countdowns, hygiene), a contextual quick-remedy strip that only ever offers something you're carrying, the medical subset of your inventory with one-tap use, and the substance ledger (tolerance / dependency / doses-vs-overdose-ceiling). It owns **no** health logic — every reading is a window onto an existing source, and the one action routes through the engine's own `cmdUse`, so a dose taken from the tablet is identical to one typed at the prompt (timed consumption, tolerance, lethal overdose and all) **Display Mode** (verb `displaymode visual|text|log`, Settings → General) is the one exception to Settings being pure client-side localStorage: it is a game-wide LADDER of three rungs ([server/engine/presentation.js](../server/engine/presentation.js)) — *visual* (graphics everywhere), *text* (the GAMES come to you as live character panels; maps and hangars stay on screen), *log* (nothing but the scrolling log). Read on the SERVER by flight (at board time, to decide whether to push a panel at all) and by poker (on sit), so it can never live in the browser. Two predicates, deliberately sharing no words so a call site cannot read as the wrong one: `prefersTextMinigames` (surfaces you ACT through — delete it and you are stuck) and `prefersLoggedPanels` (surfaces you only READ). ⚠ The middle rung is stored as `textgames`, NOT `text`: the old two-value flag used `text` to mean what `log` means now, so reusing the word would silently promote every existing text player a rung on deploy | `tablet os tabletnav tabletaction alarm codex vitals health displaymode remind wire support findbench` | `registerTabletApp(appDef)` (registry.js) — an in-memory registry other plugins call at module init to add a Home-screen app tile (`quests`/`skills`/`bank`/`weather`/`vehicles`/`properties`/`settings`/`corp` all self-register this way). `tabletnav`/`tabletaction` re-invoke an app's `buildScreen`/`handleAction` and push a fresh `tablet_panel` payload — same push mechanism ATM/Corp Console already use, no new WS routing; `tabletaction` forwards `broadcast` so an app's action can delegate into another plugin's own command dispatcher (corp-app.js → `plugins/corps`' `corp` commands). Owns `bank_transactions` (deposit ledger, `runtime`/`player` in the content registry) |
| **ideologies** | Ideology reputation display (standing + your stance/path lean) **+ the standing-, stance- and path-shifting dialogue Actions** | `ideologies rep` | The four ideologies are the NPC (`is_npc=1`) orgs: Ascendants / Long Watch / Wildblood / Exodus. Each declares a **stance** (`redeem`\|`renounce`) and a **path** (`machine`\|`flesh`\|`mind`\|`human`) in `orgs.flags` — one bipolar axis (is the world worth saving?) plus a categorical path (machine/flesh/mind are sibling ways to *ascend*; human is to *stay*). Registers `ADJUST_REPUTATION` (moves `player_ideology_rep` over `ideologies.js`), `ADJUST_STANCE` `{delta}` (the per-player `stance_axis` flag, -100 renounce..+100 redeem), and `ADJUST_PATH` `{path, delta}` (per-player `path_*` affinity flags, 0..100). All authored on dialogue options (VINE `vine-action-types.js`) — the lever behind the early "philosophical encounters." `classifyLean` scores stance-agreement + path-affinity to pick which ideology you lean toward; the raw numbers are never surfaced |
| **mutations** | **Mutations, both kinds** — radiation (involuntary, treatable, city-legible) and Wildblood mutagen (chosen, permanent, gated on Inner Circle rep AND the Quickening). Substrate is engine (`server/engine/mutations.js`); this plugin owns the verb, the trigger tick, all prose, the appearance line, the city's reaction and the active organs. **Expression 1-100** drives every derived contribution, and nothing is baked into `players.stat_*`. **Mutating is an injury** (`onset.js`): real HP to mend, stamina gone, weakness while it runs, floored at 1 HP. Natural weapons are ordinary `weaponStats` objects on the unarmed branch, never a parallel combat path. See [systems-mutations.md](systems-mutations.md) | `mutations shock screech morph swoop` | `shock`/`screech` need the organ at ≥30 expression, `morph` needs Metamorphic Body, `swoop` needs wings; all four refuse mid-turn. Registers `MUTAGEN_CONSUME` (the only path that spends a flask) and `GRANT_MUTATION` (re-checks the gate) |
| **mastery** | **The Long Watch's answer to mutation and chrome** — eight trainable disciplines (body, movement, senses, mind, combat, pain, breath, will) on a body that is never modified, because the rule the system is built on is that a Long Watch veteran **must not look supernatural on inspection**: no paper-doll entry, no appearance note, nothing an `examine` can see. Two limiters on two different axes. A **reputation gate** decides what an instructor will teach (an NPC flagged `mastery_instructor` refuses in prose that never names a tier or a flag, the same convention the mutagen gate follows). A **purity cap** decides how far any discipline can reach: every installed augment and every point of mutation expression lowers the ceiling, floored at 10 rather than 0, because chrome does not make discipline impossible, it makes *mastery* impossible. **Rank is stored RAW and the cap applies on READ**, so pulling chrome out returns exactly what you earned — baking the reduction in would be unrecoverable, which is the lesson [systems-mutations.md](systems-mutations.md) already paid for once. An instructor raises the CEILING; being hit raises the number, so there is deliberately no training dummy | `mastery train read stance technique focus` | Owns `player_reads` (what a fighter has learned about a KIND of opponent, keyed by archetype so four hundred dogs are one row) and `player_disciplines`, both `class: player`. Hydrated in one query inside the existing login batch and read from memory thereafter (**sync by contract** — the swing seam may not query), coalesced 1m flush, lazy decay at hydrate with no tick. Registers `MASTERY_TEACH` + `mastery.train`; `train` is surfaced by a declaration-only specialized action under `requiredFlag mastery_instructor` |
| **birthday** | A character's commencement date on the world's own calendar, and the age that follows from it. Shown nowhere else, and on the day itself the verb also hands over a free MANY HAPPY RETURNS soylent pouch, once per game year | `birthday` | **Derived from the account, latched once.** The in-world date is `players.created_at` extrapolated through the clock (real days elapsed × `world_clock.time_scale`), minus 25 — then written to the `birth_date` player flag and read from there forever, because the speed knob moves and a recomputed birthday would move with it. `birth_date_basis` records which rule wrote it, so the pre-extrapolation rows (which all said *today*) re-derive exactly once |
| **ascendant** | Ascendant Stronghold campus mechanics — **The Threshold** move-gate: the inner ring (`flags.ascendant_campus`, minus the public `ascension_gate` face) turns away the uncleared from either side; rushing (`player.running`) draws a turret warning. Cleared = `ascendant_clearance` flag / Ascendant rep ≥ Known / already chromed. **Plus the Rite of Ascension** — `ascend` at the Uplink, the end of the defection arc. The Rite is a *deliberate, claimed death* over the cortical economy `augments` already owns (backup → `assurance` → `backup` scan → die → print), so this file adds only the verb that kills you; everything that makes it a rite rather than a suicide happens downstream in code it does not touch. ⚠ The **wanted refusal** is load-bearing, not flavour: `onRespawnZone` declines to claim a death at 1★+, so an unrefused wanted player would die UNCLAIMED — no restore, chrome corrupted, quest stuck — and it would look exactly like it worked. Registers `ascendant:threshold` move-gate. See [systems-ascension.md](systems-ascension.md) and [ascendant-stronghold.md](proposals/ascendant-stronghold.md) | `ascend` | `ascend` is a **specializedAction** gated on furniture `flags.asc_rite` (`furn_asc_uplink_terminal`), exported for the loader rather than self-registered — a plugin exporting none of {hooks, commands, routeHandler, specializedActions} is SKIPPED by `plugins.js` and never reaches `getLoadedPlugins()`, which is how this plugin ran on import side effects alone for months. Arm-then-run confirm, deliberately the Purifier’s: the two rituals are each other’s undoing and ask the same way |
| **augments** | **Chrome as a machine you own** — the machine-path mirror of mutations, now with the maintenance half. An augment is three things that used to be one: an authored catalog row, an **ordinary inventory item** while you carry it, and an installed record once it is in you — so condition, quality, trade and looting all come free from `durability.js` and `player_inventory` rather than being rebuilt. **The schema split is the design**: every runtime column on `player_augments`, every authored one on `augments`, so neither needs an `excludeColumns`. Stats are **derived** through `registerStatContributor` (the baked `UPDATE players SET stat_x` is gone — it was the pattern `mutations.js` was rewritten away from), scaled by `(0.5+0.5·cal/100) × (1+0.25·oc) × conditionPenalty`; **calibration defaults to 100 because that reproduces the authored value exactly**, the invariant that made the migration net-zero for live characters. Surgery is a real roll by an NPC flagged `surgeon` (five bands, `botched` permanently caps calibration, `margin < −10` destroys the hardware and you still paid, charged before the roll). Heat accrues through the new `server/engine/strain.js` **sync-by-contract** substrate on the combat path — memory-only, decayed off a timestamp, no tick, and **zero condition does NOT destroy installed chrome**, it goes *dead* and waits for a surgeon. Death **corrupts** the roster into `no_install` salvage so nobody farms chrome off corpses — skipped only on `custody` (jail), **never** on a cortical restore, because the Ascendant loop now destroys the old chrome for real and the vats PRINT it again. **The pattern is who you are, never what you had**: `backup` is a ₵900 re-scan that touches no inventory, an Ascendant dies leaving an ordinary lootable corpse, and the re-print works from a roster captured at the MOMENT OF DEATH — which is what stops it conjuring an augment the player had already sold. Every re-print costs `copy_fidelity`, which caps calibration **at read time** (never baked, so a re-scan restores tuning instantly) and surfaces as visible **print artifacts** through `player.appearanceNotes` (converted `fireHook`→`gatherHook`, which had five registrants silently eating each other). `power_draw`, authored and inert for months, is now READ: draw scales with overclock, a flat cell takes chrome inert-not-damaged through the single `getAugments` funnel, and a fresh print emerges charged. An overclock fault now strikes the body — floored below `overclock_max`, **unfloored at it**, which is where the licensed/back-alley split gets a body count and exactly what the policy insures. Owns `augments`/`player_augments`/`player_backups`. See [systems-augments.md](systems-augments.md) | `augment augments calibrate calibrateresolve backup assurance` | `augment install|remove|repair|quote|overclock|charge <name>` — the first five gated to `flags.augment_clinic`, `charge` to any zone on mains; `calibrate` opens the **scoring** `aug_calibration` board (all three Display Mode rungs; client bounded to ±15 over a server `electronics` check, and a tune never lowers calibration). `calibrateresolve` is client-issued, never typed. An overclockable augment with no authored `failure_messages` **fails the build** |
| **nullcraft** | **The Null's discipline — making somebody else's machine stop working.** The fourth faction answer, opposite the augments row above: the Ascendants buy a better machine, the Null jam, spoof, lock, crash, hijack and sabotage one. The rule that shapes all of it: **it never invents a second copy of state that already exists** — `security_devices.status_flags` had held `{jammed, spoofed, hijacked_by, looping, blinded}` for years and `isWitnessed` already read it, so this PROMOTES that camera-scoped model onto bionics rather than building a parallel one. The integration is deliberately tiny: **ONE line in surveillance's `deviceStatus()`** and **ONE filter in augments' `getAugments()`**, both single funnels, so `cameraLiveInZone`, the wanted system, the feed, stats and soak all inherit Null suppression without knowing this plugin exists. Targets arrive via the **`tech.targets` gather hook** (the `workspace.provider` seam) and carry their own `apply()`, which is what keeps `server/engine/nullcraft.js` free of imports from augments/surveillance/flight. **Trace decays at read against a timestamp, never on a tick, and the engine file owns no table** — a logout drops everything; only durable sabotage persists, through the contributor's own writer. Security is DERIVED (`tier`/`calibration`/`condition`/`overclock_max`), never a second authored column. Read [systems-nullcraft.md](systems-nullcraft.md) before touching the applicability table (you cannot jam a hydraulic ram — collapsing that makes six operations one verb with six skins), the **telemetry-is-not-vital** rule, and `VEIL_CAP` (ghosting must never make a player unarrestable). Verbs are `nullscan`/`veil` and not `scan`/`ghost` because both were already taken. The minigame, Null hardware, jamming, EMP and overclock exploits are later phases; Architect's network ladder is design only | `nullscan analyze null nullresolve jammer veil emp` | The board is **NULL INTRUSION** (`null_intrusion`), and it is the only **turn-based** minigame in the client — nothing moves unless the player moves it, which is exactly the "turn-based breach" [systems-display-mode.md](systems-display-mode.md) names as the designed non-reflex equivalent it was waiting for. `nullboard.js` + the `textnullboard.js` skin (one game, two faces, gated by `textui:smoke`). `nullresolve` is fired by the client, never typed. `textRender` is called with `{ skill: 'nullcraft' }` — it defaults to `hacking` and the log rung would otherwise grade the wrong competence. `analyze` detail scales with skill; failure is never inert — trace jumps, the owner may be told, and a hard failure against defended hardware burns the deck through the existing `damageHackDeck` funnel |
| **psionics** | **The Exodus discipline — reality is more permeable than you thought.** The fifth faction answer: the Wildblood become something else, the Ascendants build something better, the Long Watch master themselves, the Null silence the machine, and the Exodus reach past all four. Phase 1 is psychometry + telekinesis. Three rules carry it. **Deniability is the progression** — codex XIV deliberately leaves psionics unconfirmed and Terminus rules that naming a thing kills it, so below Seer NO output line may claim a mechanism and at Seer the prose may be impossible; that law lives in ONE function (`voice()` in `prose.js`) with a regress seam, because a law spread across forty template strings is a wish. **The body pays** — strain is a ladder of nosebleed → blood from the ears and failing sight → seizure (`knockOut`, so you are killable where you lie) → real damage through `applyStrikeToPlayer` so the injury plugin's observers hang an actual wound off it; nothing here invents a punishment, and the backlash ladder IS the deniability ladder, which is why it broadcasts to the room. **Psychometry reads the world's own exhaust** — `residue.js` subscribes to events that ALREADY fire (`player.death`, `crime.witnessed`, `sanity.changed`…) into a decaying per-zone ring buffer; no other system was modified to feed it, and an impression is fragmentary and UNATTRIBUTED forever, because cameras answer WHO and this answers WHAT HAPPENED HERE. Also: you **major in one art and minor in another** (`psi_rank` is depth, `psi_focus` is breadth, top abilities are `focusOnly` at any cost), resistance is DERIVED from Cool/Brains/mastery/Static Mind/coprocessor with deliberately no second skill, and `PSI_CAP` is the sibling of `VEIL_CAP`. Read [systems-psionics.md](systems-psionics.md) before adding a verb — the **collision trap** cost three renames (`attune` and `pull` are engine builtins; `read` is a specializedAction in six plugins, and plugin commands beat both classes). `reach` fires `fireSpecializedAction`, so telekinesis must always DELIVER existing verbs rather than reimplement them | `psi dwell still draw reach press psiresolve ward bulwark redoubt spark burn cascade purify` | Substrate `server/engine/psionics.js` (owns no table; resonance/strain/signatures decay at read, a logout drops everything), vocabulary `psionics-abilities.js` (three build-failure contracts asserted empty by regress), `psi-resist.js` (`registerPsiResistor(fn, owner)`). Durable half is `player_flags` only: `psi_rank`, `psi_focus`, `psi_focus_second` — read via `player._flags` directly, never `getFlag()`, which is async and would break the sync contract. Sets `player._psionic`, the marker `plugins/mastery/purity.js` already reads for its PSIONIC regard rung — one-directional, mastery imports nothing from here. Board is `client/game/js/panels/psychometry.js`, all three display rungs from one `textRender` call |
| **ally** | **An NPC fights on your side.** The engine grew exactly one function for this (`npcAttackEnemy`, the last empty cell of the combat matrix); everything else here is policy. The rule that shapes it: **an ally WITHDRAWS rather than dies** at `flags.ally_withdraw_pct` (default 30) — it clears its target, sheds every enemy holding it as a target, walks home and is unavailable for a cooldown. That is a narrower window on getting your bodyguard killed, **not a shield**: a burst from 40% to zero still kills them down the ordinary `enemyAttackNpc` path, and regress asserts it. Three more decisions. **Kill credit belongs to the PLAYER** — `enemy.killed` is emitted with `actor: <player>`, `via: <npc>`, because the alternative is that your own bounty quest stops counting the moment you bring help; the seven other subscribers (accolades, corps territory, gossip, psionics residue, audio, prologue) read an ally kill as yours today, and `via` is shipped now so they can tell them apart later without a migration. Weapon-skill XP is deliberately NOT awarded — you did not swing. **Content names the targets** (`flags.ally_targets`, enemy template ids; absent = anything hostile), so the next ally anybody writes needs no code. And **a 1s plugin tick, never an AI graph**: `npcWanderTick` is 15s against a 4s swing, the graph's ATTACK node reads `entity.hit` which an NPC does not have, and `_escorting` freezes the graph outright — so an ally walking with you would never tick at all. ⚠ **No `after` in plugin.json**: the obvious `"after": ["weapon"]` re-orders every registry weapon writes to and shifted `weapon:flee` ahead of `injury:grab`, failing an unrelated plugin's suite. RAM-only, dissolves on restart, like escort | `ally` (`ally <name>`, `ally stop`) | `ALLY_ENLIST` / `ALLY_DISMISS` actions — the intended authoring route is the NPC's own dialogue firing `ALLY_ENLIST` with no params. `flags.fights_for_you` gates the **verb** only. Emits `ally.enlisted`/`engaged`/`kill`/`withdrawn`/`ended` |
| **escort** | **One NPC walks with one player** until they get where they're going — or die on the way. The rule that shapes it: **the escortee is a real NPC the whole time**, standing in the room with HP, killable by anything that could have killed it before; losing it is the tension of the job, so nothing protects it. Second consequence: **a separated escortee is never teleported** — shut it behind a locked door and you walk back for it, or doors mean nothing. Almost nothing new walks: `moveEntity` was already the single writer for every NPC tile change (doors, facades, broadcasts), so an escort step is just `moveEntity(npc, wherePlayerWent)` off `zone.entered`. The one engine change is a freeze — `npc._escorting` suppresses the AI tick exactly as `npc._aboard` already does for a charter pilot riding in a cockpit, because an escortee with a behaviour graph would stroll back to its shift between the player's steps. RAM-only, dissolves on restart like `follow`. Quests knows nothing about it: this emits `escort.arrived` and the `escort` objective type subscribes. **By air too** (2026-08-04): an escortee climbs into your plane — or a charter — through flight's `aircraft.companions` gather-hook, rides as a real occupant in a real seat, is set down where you land (`npc.transported` → an ordinary `escort.arrived`), and dies with the airframe in a crash. Neither plugin imports the other. The guard that makes it safe: **an escortee never follows a player who is aboard something**, or a landing's `zone.entered` would teleport the one you left on the departure ramp | `escort` (`escort <name>`, `escort stop`) | `ESCORT_START` / `ESCORT_END` actions — the intended authoring route is an NPC's own dialogue node firing `ESCORT_START` with no params (`context.npc` is the speaker). `flags.escortable` gates the **verb** only. Emits `escort.started`/`arrived`/`blocked`/`ended` |
| **quests** | Quest lifecycle + objective tracking. **15 objective types** (see the [plugin README](../plugins/quests/README.md) for the table): `kill`/`assassinate` (a species vs a *person*), `give`, `retrieve`, `visit`, `escort`, `deliver`, `talk`, `buy`, `sell`, `craft`, `equip`, `hack`, `spend`, `survive`. Each is a subscriber to an Event the world already emits — the systems firing them never reference quests (ADR-0002) — with three Events **added** because the act was never announced at all (`item.crafted`, `vendor.sale`, `npc.talked`). A `trackEvent` predicate may return a **number** instead of a boolean, which is the amount to add: that is what makes `spend` (counted in credits, not purchases) expressible. `survive` is the only type that isn't a single event — weather is global and carries no actor, so it's a claim about where you stood, snapshotted at the storm's peak and settled at the all-clear. **`quests.fail_on` is the MIRROR of `objectives`**: the same condition shapes judged by the same predicates against the same events, each one blowing the quest instead of advancing it — as an objective "assassinate npc_vale" means kill him, as a failure it means he must not die, which is why failure cost no new per-event code. Two shapes are failure-only: `timeout` and `escort_lost`. Authored as a **Fails if** node in the VINE quest editor (edgeless — a fail condition is live as long as the quest is); `FAIL_QUEST` is the imperative route for a failure no event expresses. **Failed is retryable by default** (`meta.failPermanent` locks it), because a permanent dead end should be asked for rather than acquired by accident. The **timeout is lazy** — derived from `started_at`, never a stored timer, so a restart can't hand out an extension; it's airtight because every path that could advance or hand in a quest checks it FIRST, so an expired quest can be noticed late but never paid out late. **`penalties`** is to failure what `rewards` is to turn-in (credits/rep/flags, no item confiscation — the package being gone is usually WHY it failed), because without it failing cost you nothing and every failure condition was cosmetic. Four load-bearing invariants from the 2026-08-04 audit, all regress-pinned: **TURN_IN claims the row BEFORE paying out** (it used to pay first and write status last, so two racing hand-ins both paid — a duplication exploit on the only thing quests give you); **`trackEvent` is gated on a live-player Set** (`_activeQuests`, hydrated at login, maintained through the single `setQuestFlag` choke point) because it is subscribed to `zone.entered` and used to cost a remote round trip **per tile walked**; it **serialises per player** (emit doesn't await, so three kills in one tick used to count as one); and `progress` **re-keys by objective id** via `player_quests.progress_keys`, because index alignment meant editing a live quest silently repointed every holder's counters. Auto-spawned `retrieve` items are tracked by row id in `player_quests.spawned` and removed on abandon/fail/turn-in — **never items a player already picked up**. **`quest track <name>` and `quest abandon <name>`** are subcommands of the log, not top-level verbs (both words are far too generic to own outright). They existed as tablet buttons ONLY — the Quests app was the sole caller of `ABANDON_QUEST` and the only writer of `players.tracked_quest_id` anywhere — so a player who did not use the tablet could take a quest and never drop or track it. Abandon is **two-step** (`… confirm`) rather than a modal: the app puts a confirm dialog in front of the same action, and a dialog is the wrong answer for a typed verb and for a screen reader | `quests quest ql` | Actions + event consumers; owns `quests`, `player_quests` tables |
| **jobboard** | **Rotating job board** — the "legal early money" path. A discovery+rotation layer over quests: posts a rotating subset of a devpanel-authored pool of repeatable quests in a zone. First board is the **Franchise Strip** (`zone_city_west`) | `gigs postings jobboard` (`gigs`, `gigs take <n>`, `gigs claim <n>`); `read <board>` (specialized action, `job_board` tag) | Owns the `job_boards` config table (devpanel *Job Boards* tab, `/job-boards` routes). Verbs delegate to the quests plugin's `START_QUEST`/`TURN_IN` — no quest logic of its own. **Actionable board:** the bare verbs and `read <board>` open the Tablet OS Job Board (Quests → Job Board). **`OPEN_JOBBOARD` action** lets an NPC (Marta) read you the same listing in dialogue. **Greeter move gate** (`jobboard:greeter`): a content zone `flags.greeter` makes a first-time NPC block your first exit once + holler (govgate write-on-block pattern). Rotation snapshot cached in a `world_flag` (`jobboard_rot_<id>`), re-rolled lazily on read once `rotation_period` elapses (no boot tick). Verbs deliberately avoid `jobs`/`board`/`take`/`claim` (owned by flight/gametable/posters/corps) |
| **deaths** | Personal death log — timestamped list of what/where each death was, plus total reclones | `deaths` | Pure `player.death` event consumer; owns the `player_deaths` table. Knows nothing about what can kill you — the killing systems (combat, weather, survival, pvp) enrich the `player.death` broadcast with `{ cause, deathZone }`; this plugin only catalogues. Total reclone count stays on `players.deaths` |
| **dev-tools** | Admin/dev utilities | `dresscyd heal lettherebelight makeitrain soil testaccolade weatherevent` | admin-only; `.makeitrain` bumps your own balance by 100k, `.heal` refills HP + the four survival meters on yourself, `.testaccolade [n]` auditions accolade banners, `.soil <player\|npc>` makes a target soil themselves — rolled severity (quiet/loud/catastrophic) drives the prose, the `bodily.sfx` cues and how many bystander NPCs recoil; a player is stained through the engine's own `stainClothing`/`soilBareSkin` channels, an NPC deliberately is not (stains live on the `players` row and NPCs have no equivalent) |
| **debug** | Testing aid — reveals normally-hidden dice rolls. `.debug` toggles a **client-only** localStorage flag (`mud_debug`); the server has no per-player state. Currently reveals IP-award rolls | `debug` | Consumes the engine `ip.roll` event (emitted by `awardIp` in ip.js) and pushes a `debug_roll` line the client renders only when the flag is on; `.debug` returns a `debug_toggle` the client uses to flip the flag |
| **container** | OPEN on containers | — | specialized action (tag-gated) |
| **wardrobe** *(also owns `dress`, and OVERRIDES the engine's `undress`)* | Saved outfits over ordinary container storage. A wardrobe is `object_type:'container'` furniture flagged `wardrobe` — every storage verb works on it unchanged; only the panel differs | `outfits` (specialized action, tag-gated) + `outfit list\|save\|wear\|delete`; `outfitsetid outfitwearid outfitwearnowid outfitdelid undressid hangwornid takeoffid` (panel-fired, not typed) | Consumes the **`container.view`** hook (fired at the end of `buildContainerView`) to retype the payload to `wardrobe_view` and hang the player's outfits off it — an unhooked container is untouched. An outfit is a list of **item template ids** (not inventory row ids: rows merge/recreate on stow-pull, and a replaced jacket is a new row), stored in **`player_outfits`** keyed `(player, wardrobe furniture, name)`. `wear` strips every body slot + accessories (never the weapon hand), then equips each piece — preferring one already carried, falling back to a copy hanging in that wardrobe, which it pulls out first. First examine-or-open of any wardrobe shimmers `outfits` per the `teachVerb` convention + ripples the wardrobe's room link (flag `wardrobe_outfits_taught`, via the **`furniture.describe`** hook). Client: `panels/wardrobe.js`, a three-column panel with a drag-and-drop paper doll — plus a **Wearing** button that seeds the doll from what's on your body (the one-click route to "save my current look") and an **Undress** button. **`dress [name]`** wears an outfit; bare `dress` with exactly one saved outfit just wears it, with several it lists rather than guessing. **`undress <wardrobe>`** strips you AND hangs everything up, honouring capacity *partially* — what fits gets hung, the rest stays shed in your pack. It covers body **+ accessories** (`OUTFIT_SLOTS`) because it pairs with `dress`, unlike the engine's body-only bare `undress`. **The `undress` override is the load-bearing risk here:** untargeted `undress` delegates straight back to the engine's exported `cmdUndress` (never a reimplementation, so the two can't drift), and an unmatched target falls back to the plain strip rather than punishing someone who typed `undress quickly`. Note `buildContainerView`'s `invItems` is `is_equipped=0`, so equipped pieces are folded into the panel's `available` set separately — without that, an outfit saved from what you're wearing renders every piece "missing" the instant you save it. See [plugins/wardrobe/README.md](../plugins/wardrobe/README.md) |
| **drinks** | Mixology and hot drinks in a **reusable** vessel. `mix` builds a drink out of ingredient PROFILES (pours, never grams); `brew` does it at a hot-water appliance; `drink` takes one serving; `pour` decants; `rinse` cleans the cup | `mix brew rinse recipes` (+ tag-gated `drink` `pour` on `drinkware`) | A finished drink lives on the vessel's `player_inventory.custom_data`, **never as a new item row** — a row would consume the vessel, and the point of a mug is that you keep it. `vessel.js` is the only writer of that shape, and the invariant is that a vessel holds either fillable's plain fluid or a `drink`, never both. **Alcohol is DERIVED** (`abv` × pours → `potency`) and applied through the ordinary `useDrug(..., 'drug_alcohol', { potencyMult })` call `applyItemUse` makes, so a mixed drink and a bottled one get you drunk identically; a zero derivation applies no drug at all. `drink <vessel>` dispatches `consume.begin` with the new `itemKind:'vessel'`, reusing the 12s sip sequence but finishing through `drinks.finishServing` instead of `finishConsumeItem` (which would delete the cup). **⚠ Registration order is load-bearing** — `drinks` must claim `drink` before **fillable**; alphabetical ordering does it and a `holdsDrink()` guard in fillable backs it up. Hot drinks gated on furniture `flags.brew_tier`; cooling derived from `hot_at`, never ticked. See [docs/systems-drinks.md](systems-drinks.md) |
| **fillable** | FILL/EMPTY fluid containers; DRINK from them | `fill empty` | specialized actions gated on the `fillable` capacity tag. Holds fluid amount + type in `player_inventory.custom_data`. `drink <container>` lands here; bare `drink`/`drink from <source>` falls through to the **water** plugin. Filling a non-empty unit splits it off the stack (filled = unique). Thirst-per-unit is a fluid property (`FLUID_RATES`, water=1), applied on DRINK |
| **fuelstation** | The forecourt: the pumps and the price board. Owns **no verbs and no prices** — a pump is a furniture row carrying `fuel_source` (which the **fillable** plugin's `fill` already reads, and which may now carry a number: ₵ per fluid unit, so a commercial pump charges while a tap stays free), and a `fuel_yard` tile is already the whole contract **trucking**'s `fuel` reads. What this adds is the SIGN. **A price on a board is never a number this plugin knows**: three systems charge for fuel with three different ideas of what a unit is, so the board renders whatever the `fuel.prices` gather hook is told by whoever will actually take the money, and a retune reaches every forecourt in the game without anybody editing a sign. Flash Point Fuel (923,907, the Yards) is the built one | — | two `furniture.describe` renderers (the pump's affordances, the board's frame) + its own `fuel.prices` row for the gasoline in the pumps |
| **flashlight** | Battery-powered handheld light — LIGHT/UNLIGHT toggle, RELOAD from a battery | `light unlight reload` | specialized actions gated on the `flashlight` class tag; item on/charge state in `player_inventory.custom_data` (`lit`/`battery`). Consumes the `visibility.perceive` hook (fired in describe.js `describeZone`) to floor the *holder's* perceived light at `clear` when lit+charged — zone lighting untouched. `1m` tick drains one charge unit per lit flashlight (online players) and kills the beam at zero. Content via `scripts/seed-flashlight.js` (`item_flashlight`, `item_battery`) |
| **doors** | OPEN/CLOSE/LOCK/UNLOCK/HACK | `hackresolve` | specialized actions (tag-gated); `hack` bypasses a `canHack` lock (e.g. hololock) via the **HOLOLOCK BYPASS** electronic-lockpick minigame — needs a hacking device, refuses while a forcefield is up, unlocks persistently on a win and emits `hololock.breached` (→ commerce vendor-grudge; surveillance charges `burglary` only if a camera/cop/bystander witnesses — the resident-NPC path is now gated by the **burglary** plugin). Also emits `breakin.attempt` on hack-arm and door-bash so the burglary alarm can wake residents. Falls through to other `hack` targets (vendor safes) when no hackable door is here |
| **drugs** | USE/INJECT, `habits` | — | USE/INJECT are specialized actions (tag-gated on `drug`); the verb is also the **route of administration** (injecting collapses the come-up and hits harder). Delegates to engine `cmdUse` → `useDrug` (phased effects, tolerance, lethal overdose, withdrawal live in [drugs.js](../server/engine/drugs.js) + [statmods.js](../server/engine/statmods.js)). **Must forward `broadcast`** so overdose-death and hallucination teleports work. `habits` formats `getDrugStatus()` — tolerance, dependency, the live withdrawal arc and the moving overdose ceiling; it **computes nothing itself**, so the read-out can never disagree with the laws. See [systems-survival.md](systems-survival.md) |
| **npc-drugs** | **Offensive NPC dosing** — the drug system's NPC-subject half (players were only ever the counterparty before). Effect is derived from the drug's own data (no content edits): `effects.hallucination`→**paranoid** (panic + flee), stimulant signature→**wired** (jittery), else→**sedated** (1 dose = loose/pacified/blurts candid lines; 2+ = out cold). | `spike jab slip` | `spike <npc> [with <drug>]` = covert Deception (Cool+Brains) check — clean + no heat on success, caught + assault heat on fail; `jab <npc> [with <drug>]` = forced needle, always lands, always `npc.attacked` heat; `slip <drug> to <npc>` = willing hand-off, only an NPC `flags.uses_drugs` (or one already loosened) accepts (the addict-economy seam). Effect state is runtime-only on `npc._ai.dose` (no NPC DB writes); engine reacts to one boolean `ai.dosedOut` (one guard line in `tickEntityAI`, same contract as burglary's `ai.alarm`); expiry + flee + flavour on the plugin's own tick. Emits `npc.attacked` for heat (reuses surveillance's `attack_npc`, no new crime content). Also a **pre-show self-dose**: any NPC with `flags.preshow_habit` (a drug name) rarely doses themselves at home when watched, off a 30-min cooldown — John Akerson's "Neural Overclock" ritual before the cameras roll (a `getNpcsByFlag` scan, no per-NPC hardcoding). |
| **dealer** | Covert shadow drug-dealer access — a spoken passphrase (not a menu) opens his trust-gated shop when he's present in your zone at his dealing hours | — | Hooks `player.say` (fired from `cmdSay`). Finds a `npc_type='dealer'` NPC with `flags.covert` in the zone; a matching `flags.passphrases` entry sets `dealer_met` (first-contact gate) and dispatches `OPEN_SHOP`. Stock tiers + trust bumps live in [vendor.js](../server/engine/vendor.js) (any NPC with `flags.trust_flag` + per-entry `min_trust`). `cmdTalk` suppresses the visible "Browse your wares" for `flags.covert` NPCs. Seed: `server/models/temp/seed-dealer.js` |
| **drugwar** | **Ambient living-world reactions** for the drug districts. No territory (`zone_control` belongs to player corps alone) and **no ideology ties** (stance/path moves only through deliberate dialogue/quest choices) | — | No tick, no event writes to other systems. `DRUGWAR_ZONES` is just the drug-district map the ambient beats hang off: **"police don't save you"** (near spawn) + **"the machine is watching"** (a camera that pans to you, streetlights stuttering, cryptic boards, a rare sourceless blackout via `drainZonePower`/`recomputePower`), per-player + server-wide cooldowns, on `zone.entered`. Content (deliberate, never on boot): `scripts/seed-drugwar-dealers.mjs` (the covert dealers, which exist independently). Pairs with the **dealer** plugin |
| **synthesis** | Also contributes the **`chembench`** Preparation Workspace provider (`workspace.js`) — the HUD's second domain, wired through the `workspace.provider` gather-hook. Its Assistant deliberately lists EVERYTHING, unlike the kitchen's: chemistry is gated on rank, not on discovery, so there's nothing to spoil. A concealed lab offers no workspace at all, the same check the cook path makes. Drug cooking + **splicing** — `synthesize` reagents into potency-scaled drugs, or `splice` (master tier) to graft multiple drugs' effects into one bespoke compound. Chemistry skill check + a "stabilize the reaction" minigame | `synthesize synthresolve splice splicepreview splicebegin spliceresolve unseal reclaim` (+ dev `cooktest splicetest`) | Reuses the crafting recipe cache + 2d8−2d8 `skillCheck`. **Cook:** `cook <recipe>` validates reagents/skill/station (chem-lab furniture `flags.crafting_station='chem_lab'`, or a `cook_kit` item anywhere at −3) → `synth_minigame`; `synthresolve` (pending+TTL) folds score+station into the margin → drug with `custom_data.potency` (read by `useDrug` to scale effects + OD weight); botch = toxic damage. **Splice:** `splice` (needs Chemistry ≥6 + a real lab) opens a designer ([splicelab.js](../client/game/js/panels/splicelab.js)) — pick a base + graft instant/phases/hallucination blocks from other drugs; `splicepreview` live-computes difficulty/instability/warnings; `splicebegin` composes + launches the harder minigame; `spliceresolve` produces an **inline-effects** compound (whole composed `effects` blob on `custom_data`, no DB drug row — carrier `item_compound`/`drug_compound`). Risks: instability→bad batch, antagonistic clash→harder, overload→`dose_weight`/low OD threshold, catastrophic→lethal blowback. Consumes a dose of each source drug + a Stabilizer. `chemistry` skill in skills.js. Seed: `server/models/temp/seed-synthesis.js` |
| **trip** | Drug hallucinations — overlay trips (scripted timed events + trippy client FX, body stays attackable) and dream-zone trips (teleport to an off-map zone + an **attackable phantom body** mirroring player HP) | — | Hooks `drug.used` / `drug.overdose` fired by `useDrug`. In-memory `activeTrips`; per-trip `setTimeout` scheduler; reuses `TELEPORT` action + `spawnEnemySync`; cleans up on logout/death/overdose/restart. Client FX: `trip_start`/`trip_event`/`trip_fx`/`trip_end` messages, `[trip]` markup, `#trip-overlay` + `.tripping` CSS, inline trip audio |
| **smoking** | Behavioural layer of smoking — appetite suppression, random hacking cough, and onlooker "cool-reactions". The stat side (Cool up, Stamina down while lit) is plain drug content; this plugin owns the rest. Nothing hardcoded to cigarettes — a drug row flagged `flags.smokeable` drives it (mirrors intoxication's `flags.alcoholic`). Cigarettes are a **pack of 10** (item tag `pack_size:10`; each use burns one via the shared engine `burnCharge`, pack destroyed on the last). | `smoke`, `give`, `accept`, `refuse` | Specialized action `smoke` (tag-gated on `drug`, flavour alias → `cmdUse`). Consumes `player.drugUsed`: sets `player.appetiteSuppressedUntil` (ms) — the engine hunger-decay tick reads it and skips decay (plugin owns field, engine reacts, posture pattern) — and broadcasts a cool-reaction to the zone. 8s self-tick rolls the cough (recent-smoke vs chronic-smoker chance). Clears state on death/logout. **Bum-a-smoke:** `give <who> a cigarette` (recipient-first grammar) is a no-requiredTag specialized action that burns one from the giver's pack and hands over a single loose cigarette — NPCs accept + thank instantly; players get a timed offer (`accept`/`refuse`, 30s TTL, giver's pack untouched until accept). Any other give (`give <item> to <who>`, non-cigarette) returns `undefined` and **falls through to the engine give**. `flags.appetite_suppress_seconds` tunes the pause. See [systems-survival.md](systems-survival.md) |
| **pipes** | **Smoking apparatus: personal pipes and shared hookahs.** Every drug until now was used up in one act — the item IS the dose. An apparatus is the opposite shape: you PACK it, spending something else, then PUFF it, several times, and not every pull need be yours. So the state that matters is on the BOWL rather than on the drug or the player, and the two verbs are its two halves. **The hookah is not a bigger pipe, its bowl is SHARED** — one person packs and everyone on a hose draws the same charges down, which is the whole reason it is furniture rather than a second pipe, and why the count lives on the furniture and not on any of the people round it: the room is sharing a number, and there has to be exactly one of it. The per-puff dose is identical, so a hookah gets the same drug into more people rather than more drug out of one pellet. The two bowls live in deliberately different places — a **pipe** keeps its on the item's own `custom_data` (personal property, pocketed and still loaded tomorrow), a **hookah** in plugin memory, because `furniture` has no `custom_data` column and a restart clearing a shared bowl is correct anyway, nobody being still sat round it. **It implements no pharmacology**: a puff is `useDrug` with `route: 'smoke'`, so tolerance, addiction, the shared depressant ceiling, overdose and the **smoking** plugin's behavioural layer all run as they do for a cigarette, and none of them know this plugin exists. ⚠ That route **requires `flags.smokeable`** and degrades to neutral without it — silently, looking exactly like it worked — so regress asserts the flag on `drug_opium` by name. And **it gates nothing it does not own**: the tempting design was "no pipe, no smoking", which means reaching into the drug path to refuse something, so instead raw resin stays chewable at the neutral route and the apparatus is simply the better one (×1.15), desirable rather than mandatory. What may be packed is decided by the **drug row** (`smokeable` or `cannabis`), never a list here, so tomorrow's smokeable is packable with no edit | `hookah`, `pack`, `puff` | Furniture `flags.hookah` is the hose COUNT; capacity is counted off the live hose map rather than stored, so there is no second number to drift. `pack` is also a declaration-only specialized action on `smoking_apparatus` so examining a pipe advertises it; `hookah` needs none, being the furniture's own name typed as a verb. A hose is a thing in your hand — `zone.entered`, `posture.changed`, `player.stop` and `player.logout` all release it, since posture is a substrate plenty of things force. Content: `drug_opium`/`item_opium`, `item_opium_pipe` |
| **cannabis** | Behavioural layer of being stoned (joints) — a **global audio echo** (all sound shimmers), the **munchies**, the **giggles**, and **bloodshot red eyes** on examine. The mellow itself (relaxation, cotton-mouth, dulled reflexes) is plain drug content; non-addictive with a non-lethal green-out. Nothing hardcoded — a drug row flagged `flags.cannabis` drives it (mirrors smoking's `flags.smokeable`; deliberately **not** smokeable, so cigarette appetite-suppression doesn't fight the munchies) | — | Consumes `player.drugUsed`: sets `player._cannabisHighUntil` (ms) and sends client `audio_echo {on:true}` → `AudioEngine.setEcho` (a master-bus delay send). 15s self-tick drains hunger + rolls cravings/giggles; ends the high (echo off) on expiry/death/logout. Owns the `player.appearanceNotes` fireHook (added in commands/world.js, mirrors `player.appearanceMisNotes`) for the red-eyes line. `flags.high_seconds` tunes duration. Content: `scripts/add-joints.js`. See [systems-survival.md](systems-survival.md) |
| **consume** | **Drinking & smoking as a timed act** — beer/cigarettes/joints consume over ~12–18s with varied narration, the drug **effect landing at the end** (not instantaneously). Owns the substance-appropriate verb ("You drain the last of the beer…" instead of the generic "You take Beer.") | — | The engine `cmdUse` offers every non-inline drug to `registerAction('consume.begin')` via `dispatchAction`; this plugin defers by **category** (`flags.alcoholic`→drink 12s, `flags.smokeable`→smoke 15s, `flags.cannabis`→joint 18s) or returns `{passthrough:true}` for instant drugs. Item is **held** (not burned) until the finish timer calls the engine's `finishConsume` (re-queried fresh) → runs `useDrug` + `burnCharge`, passing the finish line as `takeLine` (replaces the generic take-line; suppresses the come-up message). **Moving** (`zone.entered`) interrupts; a second slow consume is blocked; death/logout clears timers. In-memory `player._consume`; `player.appearanceNotes` hook shows an examine note. Narration pools live in the plugin (like smoking/cannabis), **no drug-row edits**. New engine seams: `applyDrugUse`/`finishConsume` (inventory.js), `opts.takeLine`/`opts.suppressComeupMessage` (drugs.js) |
| **rolling** | Roll **loose cannabis / tobacco** (sold by the gram — `quantity` = grams) into **joints / cigarettes**, 1 gram each | `roll` | Command verb `roll [n\|all] [cannabis\|tobacco]`. Consumes n grams of `item_loose_cannabis`/`item_loose_tobacco` → n products: cannabis → `item_joint` (stackable, merged into an existing stack); tobacco → `item_cigarettes` as a hand-rolled loose stack (`quantity` + `custom_data.loose`, the shape smoking uses for a bummed cig). Disambiguates cannabis/tobacco when you carry both. Pure verb, no state. Content: `scripts/add-loose-leaf.js` (loose leaf, `flags.form='leaf'`), `scripts/add-joints.js`, `scripts/add-cigarettes.js` |
| **smuggle** | **Raw-drug supply loop (Phase 2)** — order raw material **off a fence NPC's back-room shop shelf** (Sully the barkeep, unlocked by reaching the covert Fixer's inner circle); a MULE drone drops a **cipher-locked crate** at a far lawless airstrip (**the Scald**, `zone_waste_scald`); `unpack` it there (builds fence trust) and smuggle the raw back past a scanner **checkpoint** into the city | `unpack` (tag `mule_crate`) | **Ordering is a SHELF, not a command and no longer a node fan-out:** the fence carries `flags.trust_flag:'bm_trust'` + a `min_trust` per `vendor_inventory` entry on the named **`shelf:'back_room'`**, so the ordinary GUI shop panel renders the `bm_trust` tier ladder (`{1:0,2:2,3:4,4:7,5:10}`) and a sealed rung simply isn't on the shelf; his bar list is the unnamed front shelf and never shows contraband (`buyFromVendor` re-checks the shelf, so a patron can't buy precursor by item id). A `dealer_inner_circle`-gated root option → `bm_menu` → one `OPEN_SHOP` node with `params:{shelf:'back_room'}`. Delivery is claimed by **`registerPurchaseDelivery('mule_counter', …)`** — the engine vendor takes the money and smuggle books the **`smuggle_orders`** row *inside the same transaction* rather than handing anything over. **`trust_per_buy` is 0** — standing is earned at the gate, never bought. This replaced ~22 generated `bm_q_*` quantity nodes; `PLACE_SMUGGLE_ORDER` survives for any tree still authored the old way. **Unlock:** the Fixer's inner-circle payoff (engine `vendor.js`, now `flags.inner_circle_line`-configurable) names the fence + sets `dealer_inner_circle`, so street dealing on-ramps into manufacturing. **Delivery:** 1-min tick spawns `item_mule_crate` (owner-locked via `custom_data`; only the buyer can `take`/`unpack` it — engine TAKE checks `custom_data.ownerId`) at the drop + pings the buyer. **`unpack`** transfers the raw (safe pickup — no trust here). **Scanner = `runRawScan` exposed as the `SMUGGLE_RAW_SCAN` action** (smuggle registers **no move gate of its own**). The **checkpoint** plugin dispatches it from any gate whose config lists the `smuggle` check — the live border is the **South Gate**. Raw carried past → Deception check `3+cook_tier`; **pass earns `bm_trust` += tier** (once per ~2-min cooldown, killing the XP/trust farm), **fail = CHARGE_CRIME `manufacturing`** + bounce + 45s heat. Scanner sees **bagged** raw (surveillance `rawAmong` filters `container_id IS NULL` — bags hide raw on the street, not at the border); a **sealed crate** is `raw_drug`@`cook_tier 5` so hauling it whole is the hardest sneak. **Economy:** the Fixer/Sully carry `flags.drug_buyer` → vendor.js pays `0.7×value×potency` for finished drugs (a skilled cook profits; a botch doesn't). Content: `add-blackmarket-fence.js` + `add-reagent-supply.js` + `build-smuggle-funnel.js`. Needs `npm run db:schema`. See [systems-economy.md](systems-economy.md) |
| **checkpoint** | **Content-driven security-checkpoint law** — one `registerMoveGate('checkpoint')` configured entirely by a `flags.checkpoint_cfg` object on the gate tile; the ONLY checkpoint move-gate in the codebase | — | Config `{ guards, checks:[wanted\|contraband], wantedMode:hard\|bluff, + one entry predicate insideFlag\|fromFlag\|fromDistrict }`. Walking ONTO the tile runs the configured checks in order (first block wins); the entry predicate gates *which side* is policed (skip if arriving from `insideFlag`; only check if arriving from `fromFlag`/`fromDistrict`). **wanted** (reads the `wanted` player flag): `hard` = flat turn-away for any star; `bluff` = **Deception** check diff `5 + 3×stars`, pass talks through (no XP farm), fail = **APPREHEND**, and ≥4 stars is unbluffable (straight detain) + 45s heat. **smuggle** = routes raw-drug through the **smuggle** economy via the `SMUGGLE_RAW_SCAN` action (cook_tier-scaled Deception, `bm_trust` reward, manufacturing bust, bags-see-through) — one source of truth, gated by this checkpoint's entry predicate (so, unlike the old direction-blind smuggle gate, only the policed side is scanned). **contraband** = the generic scan (raw_drug/`contraband` tag) → **Deception** diff 7, fail = **CHARGE_CRIME `contraband_possession`** + APPREHEND (kept for the gov recipe). No verbs/tables. **Shipped recipe:** the South Gate (`zone_district_918_919`, `fromDistrict:wilds`, `checks:[wanted,smuggle]`, `wantedMode:bluff`) — catches fugitives coming back cityward AND forces raw drug to be smuggled past on the way in. **Dormant recipe** (North City rebuild): `{ insideFlag:gov_enclave, checks:[wanted,contraband], wantedMode:hard }`; `scripts/add-gov-checkpoint.js` still flags the enclave tiles. See [the-under.md](proposals/the-under.md) |
| **trade** | **Two-sided trade window** — the default UI for safely swapping items AND credits between two players (a mutual exchange; `give`/`pay` remain the one-way shortcuts) | `trade tradeoffer traderetract tradeready tradecancel` | `trade <player>` (same room) sends an invite (clickable **Accept** `action-link` → `trade <you>`); accepting opens a shared **two-panel window**. The **server owns all state and renders the whole panel HTML**, pushing `trade_update {html}` to each side (poker pattern) → client `panels/trade.js` injects it into a `#trade-overlay` modal and relays button clicks as `tradeoffer`/`traderetract`/`tradeready`/`tradecancel`. Each side stakes items (`tradeoffer <invId> [qty]`, partial-stack split at execute) and/or credits (`tradeoffer credits <n>`); **any change unlocks BOTH sides** (anti-scam); when both `tradeready`, the swap runs as ONE `withTransaction` (items both ways + `adjustCredits` both ways, all-or-nothing, re-validated at execute). No escrow, no table — sessions are in-memory; leaving the room / logout cancels. `trade_close` hides the panel. No schema  **The log carries the record.** Staking, retracting, locking and unlocking used to produce ZERO log output (four of five `pushBoth` sites passed no message) and the panel buttons submit silently — so closing the window mid-trade left no trace of what had been agreed. Every state change now writes both offers to the log for EVERY player, and `trade status` re-reads them on demand. That is a correctness fix, not an accessibility one: the anti-scam rule (**any change unlocks both sides**) is worthless to someone who cannot see what the other side staked — they are being asked to lock in blind. At the bottom Display Mode rung no panel is sent at all and the log IS the trade window |
| **pay** | **Player-to-player credit transfer** by mutual agreement — the safe, agreed-upon way to hand cash over | `pay acceptpay declinepay cancelpay` | `pay <player> <amount>` opens a same-room offer (SIFT-resolved recipient); the recipient `acceptpay`s (or `declinepay`s) and the debit+credit happen atomically in one `withTransaction` (`adjustCredits` both sides) — **no escrow**, so funds are re-checked at accept time (nothing sits in limbo across a restart, payer can't double-spend). 60s TTL, in-memory `pendingPay` map keyed by recipient. `cancelpay` pulls your own offer. No tables |
| **sportsbet** | **Player-vs-player betting on the live baseball broadcast** — pick the winner (a called exact score trumps), stakes escrowed, pot paid at the game's final | `wager takewager cancelwager` | `wager <player> <amount> <team> [away-home]` proposes a bet on the airing game; opponent `takewager [away-home]` takes the other side. Consumes the **`sports.game`** event (emitted by the **broadcast** plugin when a fresh airing starts, carrying the already-decided result + `endsAtMs`). On lock, both stakes are debited in one `withTransaction` and a row is written to **`sports_bets`** (outcome captured now, since the game is decided at airing; `resolve_at` = the broadcast final). A **1-minute sweep** settles due bets (restart-safe — no per-bet timers), pays the pot (exact-score call trumps winner-pick; tie/both-exact = push/refund), and notifies both with winner + final score + bet details. Needs `npm run db:schema` (`sports_bets` table) |
| **sportsleague** | **One league per sport** — computed standings + monthly seasons climaxing in a single championship game: DEADBALL's World Series and the CPhL's **Coldwater Cup** | `standings worldseries cup champions` | **Zero-write standings:** every game is a pure function of its slot, so a league table is a *computed fold* over the season's slot window (`broadcast.computeStandings`), cached here and recomputed only when the slot advances — no per-game rows and no upserts. (The older row-per-game booking into `sports_standings` is gone; the table remains only as legacy schema.) `standings` prints baseball sorted by win % → wins → run diff, hockey by points → regulation wins → goal diff. **Runtime** state (not content — excluded from `CONTENT_TABLES`/exports; regenerated by play). Booking is **end-of-game + restart-safe**: each aired game (`sports.game` event) is staged as a `pending` row in **`sports_results`** (game_id PK) and a 1-min sweep books it into `sports_standings` once its airing window ends — so only finished games count, downtime is caught up, and a re-simmed same-slot game (broadcast keys game ids to an absolute time-window) no-ops instead of double-counting. Exposes **`sportsleague.getStandings`** + **`sportsleague.getSeason`** Actions (both take an optional **`sport`**, defaulting to baseball) so **broadcast** can flash the **standings-bug** overlay, work records into the announcer's pre-game (`matchup.records`), and run each league's postseason takeover. **Per sport:** `sports_season` carries a `sport` column keyed `(sport, season_no)`, each league folds its own stats (baseball W-L-RD; hockey W-L-OTL-PTS plus a scoring race and a casualty count, defined by `SEASON` in `plugins/broadcast/sports/<name>.js`), and `standings [sport]` / `champions [sport]` scope to one league or print them all. `cup` is hockey's `worldseries`. **Seasons (one per sport):** a 1-min tick opens a season per in-game calendar month for every league with a show on the air; when the month rolls (runner-up ≥ `SPORTS_MIN_WS_GAMES`), the top two seed into a **single championship game** — pinned to that league's own airtime, and its takeover applies only to its own sport — broadcast runs *those two* with gold championship branding (`worldseries.*` pools + WORLD-SERIES/CHAMPION graphics); the result crowns the champion, wipes standings, opens the next season. `worldseries start` (dev) force-seeds for testing. Owns `sports_standings` + `sports_season` (both **runtime**, excluded from `CONTENT_TABLES`). Needs `npm run db:schema` |
| **food** | EAT | — | specialized action (tag-gated) |
| **cooking** | Turning raw/frozen food into something safe — and, for food that opts in, something *good*. **Two tiers:** plain `needs_cooking` food is binary (heat → wait → cooked, unchanged); food tagged `food_profile` gets a **peak window**, a **burn point** past it, **handling that matters**, and a **quality band** **Improvised dishes:** an unmatched pan is no longer slop. `improvised.js` infers a FAMILY from the vessel + profile multiset (curry/chowder/stew/soup/broth in a pot; pie/bake/gratin/roast in a tray; hash/scramble/sauce/sear/saute in a pan; salad/mash/dip in a bowl; grill bare) and names it after its primary ingredient — "beef stew", "apple pie". The rule is **food makes a dish, non-food makes a mess**, so slop is now only what a pan with something inedible in it produces. Improvised ceilings climb with COMPLEXITY (distinct profiles, modifiers excluded) and stop at `superb` — **`masterful` belongs to the authored catalog**, and `RECIPE_MASTERY_IP` pays a flat bonus for plating a known recipe at excellent+, so knowing the real thing always beats inventing near it. **Player recipes:** an improvised dish carries `custom_data.improv` (its signature); `recipe save <name>` writes it to `player_flags` `recipe:<slug>`. Identity is the SIGNATURE, the name is a label — so renaming is free, saving one combination twice is refused, and seasoning isn't part of it. Shared two ways: `recipe write` mints a tradeable `item_written_recipe` card (`read` learns it), `recipe teach <name> to <who>` for someone in the room; the author travels with it either way. **Shopping list** (`shoplist add|tidy|drop|clear`, `shoplist.js`; `drop` takes a line NUMBER or a whole RECIPE name, mirroring `add`): one `player_flags` row of what you're SHORT of. **It stores what you want, never what you have** — every tick is derived from your inventory at read time, so buying the onion crosses it off with no write firing anywhere and the list can't go stale. Entries are ingredient CLASSES, which is what lets the **`shop.stock` hook** mark whatever a given vendor happens to stock that satisfies one. Read by the verb, the Cookbook tablet app's list screen, and the shelf marker. | `cook plate flip stir stove doneness mise taste score tenderise marinate chop cut butter mince cure deglaze scour kitchenkit microwave recipe shoplist drain` (+ `heat`, `prep` and `tenderize` aliases) | **No tick, no polling.** The session is a timestamp blob on `player_inventory.custom_data.cooking` (`{applianceId, startedAt, thawMs, cookMs, doneAt}` + `{profile, heatTier, vessel, acts}` when profiled); the peak/burn windows, current stage and final band are all *derived* from it and `now` at the moment somebody asks — examine writes nothing. **`mise`** (alias `prep`) gathers those same derived reads into one board — every vessel and where it sits (in hand / stowed / on which burner, at what heat), what's in each and how far along, plus loose ingredients and their prep state — because prep lives on the ingredient, heat lives on the appliance, and the dish lives in whichever pan you happen to be holding. **Writes: one per `heat`, one per interaction, one per `plate`.** Bounded `setTimeout`s narrate stage beats and fire the burn-off, rebuilt from `startedAt` by a boot-catchup IIFE (jail's release-timer pattern) — losing them loses flavour, never state. **Appliances:** room stove (`flags.stove_tier` low/mid/high, power-gated, held `busy_until`) or a carried `tags.portable_oven`; **vessels** are ordinary carried containers (`tags.vessel` + `heat_distribution`/`heat_retention`) reusing `player_inventory.container_id` — `heat <pan>` heats what's in it, and cooking bare on the stove works but cooks worse. **Handling** (`flip`/`stir`) is carry-gated on a `can_turn`/`can_stir` tool (the fishing-rod pattern), never a `+N` bonus. **Quality:** each profile maps an end state to the best band it can reach (a tomato is excellent raw *and* cooked; a potato raw is poor) — that's a **ceiling**, and heat/vessel/turning/timing + a `cooking` skill check decide how far below it you land. Band → `custom_data.cook_quality` (in `INSTANCE_KEYS`, so a Masterful steak never stack-merges into a Poor one), spent in exactly one place: `applyItemUse` scales `restore_hp`/`restore_hunger` (poor 0.5× → masterful 1.6×; **absent ⇒ 1.0**, so nothing predating this changed). `plate` awards Cooking IP via `awardSkillUse` + a flat `grantSkillIp` bonus for Excellent/Masterful. Profiles are plugin-local (`profiles.js`), gated by `validateProfiles()` in regress. Adding a food = **one tag**. See [plugins/cooking/README.md](../plugins/cooking/README.md) |
| **workspace** | **Preparation Workspace** — a domain-agnostic text HUD over a working area. Gathers providers via the `workspace.provider` hook, builds one `workspace_view` payload (Storage / Preparation Area / Components / Tools / Status) and ships it to `client/game/js/panels/workspace.js`. **Holds no gameplay logic** — it renders state other plugins own, and every action it will offer is a verb string a player could have typed. **All six phases built, TWO providers** (`kitchen` from cooking, `chembench` from synthesis — the second added with **no change to this plugin or its client panel**, which was the whole claim). A room that is both shows a chip for the other; priority settles the default (stove 20 › chem lab 15 › dish-cabinet-only kitchen 10). The panel renders, every component carries the **context actions** that apply to it — each one a literal verb string (`chop rat haunch`, `stow onion in pot`, `stove low`), regress-checked against the live command registries. Clicking runs `sendCmd` so the command **echoes into the log as though typed** — the panel teaches the verbs rather than replacing them; a trailing space marks a PREFIX (`marinate x in `) that fills the input line instead of firing. Provider gates are deliberately COARSE (profile family / raw-cooking-cooked / tool in reach) — the HUD proposes, the verb decides, because re-deriving `score`'s real preconditions here would be the duplicate implementation this layer exists to avoid. The **Recipe Assistant** scores completion + the real-weight shortfall for each recipe, sorted Available Now / Nearly Available / Missing Ingredients / Missing Equipment — **only for recipes the player KNOWS**, because listing the undiscovered half with exact counts is the ingredient checklist the Cookbook app deliberately refuses to be (it reports the rest as a bare count, same sentence). Scored against `matchScore`'s own tolerance-adjusted floor, so *ready* and the matcher can never disagree. **Click a recipe** to open its ingredient list and numbered method, and to highlight (`▸`) the exact rows it would use wherever they are — fridge, cabinet, or your hands. `uses` comes from the SAME picker `prepare` runs (`pickFor`), with a regress case on it: two implementations of "which onion" would show one row and hand you another. Selecting costs no round trip, and is **multi-select** (the union across everything you have open). **Take the marked** pulls every highlighted row out of room storage in one click; a recipe you're short of offers `+ list` instead of `prepare`. The panel **polls only while something is cooking** (5s), and is silent otherwise. No writes of its own, no tick, no server-side panel state (closing is client-only; refresh is the client re-issuing `workspace`, the container panel's loop). Cooking contributes the `kitchen` provider from [plugins/cooking/workspace.js](../plugins/cooking/workspace.js) — two queries, everything else in-memory. A chem or gunsmith bench contributes its own without either side importing the other. See [plugins/workspace/README.md](../plugins/workspace/README.md) and [docs/proposals/preparation-workspace.md](proposals/preparation-workspace.md) **`prepare <recipe>`** runs a provider PLAN — an ordered list of ordinary commands walked one at a time, stopping on the first failure and never answering a SIFT prompt. **Nothing is reserved**: re-validating at every step is what makes a soft-lock unnecessary (trade refuses escrow, crafting consumes at commit — no path in this game locks an inventory row). The kitchen planner stops at a **loaded vessel and never cooks** — heat is where the skill is. | `workspace bench prepare` | `workspace.provider` (gather; returns `{key,label,priority,build}`), `workspace.view` (decorate the payload in place, same seam as `container.view`) |
| **water** | DRINK from furniture | — | specialized action gated on the `water_source` capability tag; resolves a named (or any) water-source furniture in the zone, restores thirst, else falls through to item DRINK. `wash` is owned by the **mis** plugin (it recognises `water_source`, not just `object_type='sink'`) |
| **lighting** | SWITCH/FLIP/TURN on switchable lights | — | specialized actions (tag-gated) |
| **weapon** | **Player-initiated combat**: `attack`/`kill`/`k` target resolution (enemies → NPCs → destructible infrastructure → doors → players), the player swing (`resolveAttack`/`resolveAttackNpc` — kill handling, corpse + on-corpse loot creation, `enemy.killed`/`enemy.attacked` emissions), and sleep-kills (`offlineSleepSwing`). Initiating an attack force-clears any non-standing posture. Ambiguous PvP targets replay their SIFT pick through the `ATTACK` Action (`dispatchType`/`dispatchParam` — the builtin replay path doesn't cover plugin verbs). Also owns **combat stances** (`fight <stance>` — 5 stances trading hit/swing-speed/defense on a 60s lock), the **`pow`/`dodge`** moves (shared 10s window; `pow` is a wind-up that *resets* the swing timer to 1.5× for 250% damage — it does not wait on the attack cooldown, which in sustained combat is almost always running — vs. `dodge`'s +5 defense that also locks you out of attacking), and the **contested flee** move gate — see [combat.md](combat.md) | `kamehameha seppuku fight pow power dodge flee` | specialized actions `attack kill k` → dispatch the `ATTACK` Action, plus `reload`. **AMMUNITION** lives here: any weapon tagged `ammo_capacity` spends one round per SWING (hit or miss), refuses **before** the swing when dry so being empty costs the shot rather than the shot and the cooldown behind it, and is refilled by `reload` from a carried item whose `ammo_load.type` matches the weapon's `ammo_type` — matched by TYPE, never by item id, so two weapons can share ammunition with neither edited. An **absent count reads as FULL**, so a weapon that arrives by any route (bought, looted, granted) is loaded; and nothing authored before the tag existed carries it, so every other weapon in the game still fires forever. ⚠ `getEquippedWeapon` **caches** the row on the player, so the spend writes through the cached object as well as the DB — the DB write alone would let the next swing inside the TTL re-read the old count and the magazine would never empty. `reload` is shared with **flashlight** and self-gates by returning `undefined` when the equipped weapon takes no ammunition. Also spawns corpses for `result.arcKills`: an `arc_chain` weapon kills things it was never aimed at, and the engine returns loot but never a body. Injects the auto-attack provider via `registerPlayerCombat`; injects the auto-attack provider via `registerPlayerCombat` (engine combat.js) — gameLoop's 1s tick calls it as raw function refs, per ADR-0001, and also drives the flee retry through it. Registers move gate `weapon:flee`. Combat *math* (`playerAttackEnemy`, `pvpSwing`, cooldowns, the stance table in `stance.js`, the flee contest, enemy AI) stays engine |
| **butchering** | Timed corpse carve — posture-based (`posture === "butchering"` is authoritative, companion `player.butcherState`), 5s carve, per-cut Butchering skill check, botches ruin the part and bloody the butcher | `butcher` | Registers the `BUTCHER` Action — the typed command and the engine loot router (loot on an empty-but-butcherable corpse) both dispatch it, so the loot↔butcher coupling is purely through the action registry. 1s tick completes/cleans up; `player.stop` listener; force-stand interruptions inherited from posture |
| **warmth** | **Heat sources.** Battery-backed space heaters that HOLD a room at `flags.heater_target_c` (a thermostat, not a bonfire: self-limiting, and two are not twice as warm), running 12 in-game hours off `flags.heater_battery_min` once the grid drops — the answer to HVAC dying in a blackout. Charge is RAM, flushed in 10% bands. Also owns the `warming` consumable tag (hand warmers). Registers a heat source with the engine so the drift, frostbite and the HUD all agree. On/off is `plug`/`unplug`, inherited from **appliances** | — | tick + `item.consumed` |
| **clothing-wetness** | Per-item wetness from rain/snow, body-temp effect. **Bare skin wets too** (and dries 8× faster); **sleepers included**, reading `bodyZoneOf` so a dreamer's body gets rained on | — | tick + hook |
| **frostbite** | Peripheral cold injury — the hands/feet/ears a cold body sacrifices to hold its core at 37°C, which is why a superb coat used to make −30°C free. A 0–100 meter off the **windproofed apparent temperature with no credit for core insulation** (a parka does nothing for your fingers) × `player.extremityExposure`; stages frostnip/frostbite/deep register status effects taking −1/−2/−3 Reflexes, stacking with the core cold penalty. Full cover is slow, never immune. Not while submerged. **Deep frostbite is PERMANENT** — crossing 90 latches a floor and it thaws only *to* that floor; a `treat_frostbite` field kit walks it back but never clears it, and only `CLINIC_TREAT` lifts the floor (billed per stage). The one permanent injury in the game. Persists to `player_flags.frostbite`+`frostbite_floor` on **stage change only** | — | tick + `item.consumed` |
| **weather** | Seeded 7-day forecast (type, temp, wind kph, humidity %); owns `weather_forecast` table. Daily wind rolls from the climate profile's `monthly_wind_kph` scaled by weather type + a windy/calm roll; humidity from `monthly_humidity`, driven hard by type. The engine turns temp+wind+humidity into **apparent temperature** (`getZoneApparentTemperature`, environment.js — wind chill / heat index) feeding body-temp drift and comfort messages. Also owns the drifting per-zone **weather field** (cloud/storm cells over `map_world`, drift ∝ wind; re-derived from the day's seed, no table) — injects `sampleWeatherAt(x,y)` via `registerWeatherField`; the engine samples it in `getZoneTemperature`/`getZonePrecip`/`getZoneStormIntensity`/`getZoneVisibility`. Outdoor look descriptions append wind (`getWindDescription`) and air-feel (`getAirDescription`) lines. The engine emits `weather.zoneAmbience` per occupied outdoor tile; the **audio** plugin runs precip + wind ambience beds off it, with a thunderclap duck. Also owns the **extreme-weather `severity` scalar** (per forecast day + per field tile, via `getZoneSeverity`) and the **named "hero" event** layer (`NAMED_EVENTS` — ion storm / acid rain — with an approach→peak→passing lifecycle that forces a severity preset; driven by the engine's 30s tick via the `registerWeatherEventStep`/`registerWeatherEventTrigger` seams, dev-triggerable at `POST /environment/weather/event`). See [systems-weather-extreme.md](systems-weather-extreme.md). | — | tick (30s advect + event step) |
| **audio** | Procedural audio runtime — asset CRUD (`audio_instruments`/`songs`/`sfx`/`ambient`/`samples`) + the event-route table; zone/SFX event playback; weather precipitation ambience beds and thunder ducking | `.createsound .playsound` (admin) | Emits WS `audio_ambience` (per-tile weather beds via `weather.zoneAmbience` 30 s tick + `zone.entered` top-up through `reconcilePlayerWeatherAmbient`, with synth fallbacks for rain/sleet/snow/blizzard) and `audio_duck` (`weather.thunder` sidechains the weather bed). Client `audio-engine.js` exposes `duckLoop(id, fraction, holdSeconds, rampSeconds)`. In prod (`CONTENT_READONLY`) the library **and** sample blobs are read from the `content/` git checkout on disk, not the DB — saves ~18MB of Neon egress per reboot; dev still reads the DB (local WIP). See [amp-format.md](amp-format.md) |
| **zone-validator** | Zone exit-connectivity integrity checks | — | startup/validation |
| **atm** | ATM terminals — power-aware, faction-networked, hackable, finite cash stock | `atm deposit withdraw jack jackresolve drain` (+ admin `.hackpreview`) | USE specialized action (tag `atm`); replenish tick every 5 min. Owns `deposit`/`withdraw` outright — the matching engine `economy.js` handlers were removed. `jack` requires carrying `item_hack_deck`; the Circuit Breach minigame result (via `jackresolve`) is authoritative — no server-side skill roll — and unlocks per-player MAINTENANCE access on that terminal; `drain` cashes it out into carried credits and bricks the machine (named to avoid the broadcast plugin's `eject` verb) |
| **bounty** | Player-funded contracts on other players' heads — [systems-bounties.md](systems-bounties.md) | `bounty bounties redeem` | READ specialized action (furniture tag `wanted_board`) that **self-gates and falls through** when there's no board in the room — `read` is shared with recipe cards, books and the leaderboard, and an unconditional handler eats all three. `furniture.describe` hook telegraphs the board. `player.death` mints a **real `item_bounty_head` inventory row into the corpse** (only when a contract is open), so the payout follows the OBJECT, not the kill. Escrow lives on the `bounties` row; a claim closes it with a guarded `UPDATE … WHERE status='open'` before a credit moves. Write-through in-memory mirror (this plugin is the table's only runtime writer); 5-minute idle-gated expiry sweep over the Map, no tick query. Registers the tablet **THE BOARD** app + an `alwaysOn` widget when paper is out on you |
| **bulletin** | Town-square leaderboard board — `read` it for the top 5 survivors by total XP | — | READ specialized action (furniture tag `bulletin`); ranks `bonus_xp + SUM(player_skills.ip)`, ties broken by older `created_at` |
| **shove** | Force a player or corpse into an adjacent room | `shove drag` | Contested 2d8 of actor brawn vs. target carried-weight(kg)/3 (rounded up). Validates the exit first; on success reuses engine `cmdMove` for both parties with `{bypassEncumbrance:true}` (relocates corpses via `moveCorpse`); 60 s cooldown only on failure. Player gating mirrors `attack` (forcefield apartments block) |
| **drama** | Dramatic entrances — one authored line that replaces your arrival message in the next room you walk into | `drama` | `drama <text>` stores the line in `player_flags.drama_line` (`$player` → your handle; `<`/`>` stripped, 240 chars) and arms it; bare `drama` re-arms; `drama off` clears. Arming is runtime-only (`player._dramaArmed`) and **one-shot** — the `movement.arriveMessage` hook consumes it and hands the line back to `cmdMove`, which uses it in place of "X arrives from the south" |
| **search** | **The `search` verb — turning a room over by hand.** One `scavenging` check (there is no perception skill and this was deliberately not the place to invent one), and results contributed ENTIRELY from outside through the **`search.provider` gather hook** — the `workspace.provider`/`zone.smells` seam, so a contributor declares the hook in its own manifest and imports nothing (an exported registry would make every findable thing import this plugin, and need an `after:` on each one to avoid a registration race). Lowest `priority` wins and is the ONLY hit reported: a search turns up one thing, and listing every hit at once would tell the player exactly how much was left to find. Three rules. **It never pays out** — the 30s cooldown is per-zone, so on a district grid any loot-granting provider is a faucet limited only by walking pace; providers return KNOWLEDGE, and if you want loot you extend the scavenging tables, which are stocked per zone and deplete. **A failure is indistinguishable from an empty room** — no near-miss line, no "something is nearby", no partial reveal; regress asserts every failure line against a leak pattern, because one hint turns every provider in the game into a detector and you could no longer tell an unlucky search from a barren one, which is the entire feeling the verb exists for. **It is not a posture** — `scavenge` owns the perpetual settle-in hunt, this is one deliberate look, and a tick here is rebuilding scavenging. Providers today: `strays` (the cat) and `concealment` (that a disguise piece IS one — never the code, never what is behind it) | `search` | See [plugins/search/README.md](../plugins/search/README.md) |
| **strays** | **Cathode, the stray cat with a bionic paw** ([systems-strays.md](systems-strays.md)). Hides in Dray Lane, surfaces into an occupied lane tile for 45–120s at a time, does one of 32 gated behaviours per beat, and **remembers who petted it and who killed it, permanently**. Four decisions carry it. **Hiding is ABSENCE, not suppression** — while hidden it is in a real, exitless, off-map den zone, so `look`, `getZoneNpcs`, SIFT, `attack` and `pet` all agree for free because there is only one truth about where it is; a `flags.hidden` honoured by the room-description filter would need honouring by six other readers, and the first one anybody forgot is an invisible cat you can still stab. **The den is also the fail-safe** — it is the NPC's `home_zone`, so the engine's own boot placement (`world.js`) and its own respawn (`gameLoop.js`) both return it to hiding *without knowing this plugin exists*, making "hidden" the default state after any restart, crash or unanticipated path. **We RETUNE the engine's respawn timer rather than replacing it** — `combat.js` sets `_respawnAt = now + 60s` on any NPC death, and the 24h hide is that same field with a bigger number, written as the first SYNCHRONOUS line of the kill handler; let one `await` creep above it and `npcWanderTick` fires in the gap, the cat is back a minute after it died, and the entire grief window silently becomes sixty seconds with nothing else in the codebase noticing (regress asserts >23h, and the `stray_cat_hidden_until` world flag is an independent second mechanism covering the restart case the RAM field cannot). And **the memory is split across two stores on purpose** — warmth lives in `player_npc_relations` for tiering, decay and VINE conditions, but the KILL is its own player flag that never decays and outranks everything in `moodToward()`, because warmth is a shared substrate that buying things and being clean both nudge, and killing this animal must not be launderable by being pleasant for a fortnight. Petting pays +6 sanity on a 6-hour cooldown — inside the cooldown the pet still WORKS and still reads warmly, because you never punish a player for petting a cat, you just don't pay them twice. Killing is ordinary `attack` with deliberately **no `no_attack` gate** (that would make it a statue with a personality) and costs the killer 18 sanity, every witness 6, and warmth with every NPC in the room. Also **calling her**: `Cathode!` / `Here Cathode!!` / `call cathode` / `summon cathode` / `pspsps`, anywhere in Coldwater. An INPUT MATCHER, not a command, because bare `call`/`summon` are gametable's poker verbs and **SIFT does not arbitrate verbs** (it resolves which TARGET you meant; its `context.verb` is only a replay key) — so every pattern but `pspsps` carries her name and poker keeps its verbs, with regress asserting bare `call` is NOT matched. **Spamming deliberately does not help**: flat chance, no streak or pity counter, 60s between attempts AND 45min after she actually comes, because 60s alone is ~60 rolls an hour and any workable chance makes that a reliable summon. Only the RELATIONSHIP moves the number (4%/9%/20%, never for a killer — who gets the ORDINARY miss line, since a bespoke one would tell them what they did). Read the **`zone.npcs` reconciler is a net, not a licence** note before touching any movement: `reconcileNpcMembership` now sweeps npc-set drift on the same 30s tick as the player one, but it believes `npc.zone_id` — so it repairs a drifted SET and is blind to a wrongly-written FIELD, and 30s of a cat in two rooms is still a cat in two rooms | — (hooks only) | `npc.petAttempt` + `search.provider` hooks; consumes `npc.killed`/`zone.entered`, emits `npc.petted`/`stray.found`. The engine's `cmdPet` gained the hook and the emit (same `undefined`-falls-through convention as the `npc.talk` hook above it); the plugin CLAIMS the pet, so the engine's emit never runs and the plugin fires `npc.petted` itself — exactly one per pet. NB `cmdPet` passes **spread copies**, not the live NPC |
| **scavenging** | Perpetual, posture-based search — per-zone loot tables, the 2d8−2d8 Scavenging check, lazy weighted replenish | `scavenge` | Sets `player.posture="scavenging"` + runtime `scavengeState`; engine reads it in `describePlayerAppearance` and clears it via the usual force-stand triggers. 1s plugin tick drives 3.5s attempts. Owns `scavenging_tables`/`_items`/`_zone_stock`/`_zone_state`. See [systems-scavenging.md](systems-scavenging.md) |
| **fishing** | Posture-based cast-and-wait; a bite arms the client tension-bar reel overlay (`fishing_game`); landing pulls a catch, gated by the 2d8−2d8 Fishing check | `fish` `fishcast` `fishresolve` | Sets `player.posture="fishing"` + runtime `fishState`; 1s tick drives 4.2s casts, pauses on an armed bite (anti-spoof token) until the overlay reports via `fishresolve`. Rod carry-gate (`fishing_rod` tag) + optional `bait`; monster hooks `spawnEnemySync`, botched reels snap the rod. Zone opt-in via `flags.fishing_table_id`; reuses the scavenging table schema. See [systems-fishing.md](systems-fishing.md) |
| **climbing** | **Rock faces with a way up them** — the gear half of the impassable-terrain law ([plugins/climbing/README.md](../plugins/climbing/README.md)). ~737 tiles across Terminus, Deadwater and the Scarletwastes were reachable only with mutation wings; the whole southern and eastern outside of Terminus (640 tiles, over half that region) was one of them. The rule cliffs carried for months was **"no climb, deliberately, and no GEAR exemption"**, and it still stands — what changed is that the exemption is a property of the **TILE**, not of the gear. A bare `cliff` is absolutely impassable and nothing purchasable will ever open one (regress asserts it *with the rack in hand*); a **`scree`** tile is PAINTED, with its own terrain, fill and name, so "a player can look at the map and KNOW where the ways through are" survives intact. And passage is **DETERMINISTIC** — never a roll, because "sometimes" is exactly what the old rule forbade; the Climbing skill scales the COST the way Swimming scales a stroke. Twelve routes are painted, each named for what it is (The Notch, Split Rock, The Chimney…) | — | Fills the engine's **`registerClimbProvider`** seam ([movement-gates.js](../server/engine/movement-gates.js)) — a gate can only ever VETO, so the climb exemption could not itself be a gate; the law ASKS instead, and **fails closed** (no provider ⇒ a climbable tile is an ordinary cliff). `on('zone.entered')` charges the stamina — never the check, which a later gate could still veto. Gear is any `climbing`-tagged item carried uncontained (the `boat`/`rebreather` test). `claws`/`extra_limbs` add to effective skill; `flight` never reaches it. No verb (**flight owns `climb`**), no table, no tick |
| **swimming** | Water tiles are swimmable, not walls — stroking between water tiles costs stamina (Swimming skill = Endurance+Brawn), treading bleeds it, 0 stamina → `drowning` status effect; diving to `flags.underwater` tiles arms a breath timer. Soaks you (wetness) + cold-water hypothermia via the body-temp path | — | `on('zone.entered')` charges the stroke + owns `player._submerged`; a 1s tick treads/drowns/ambiences. Retires the `engine:water` gate + the insta-drown failsafe. Boat-tagged item = ride dry. **Vessels:** a `flags.vessel` zone closes the water tile under its hull (`swimming:vessel-hull` gate) and is climbed from alongside via the `VESSEL_EMBARK`/`VESSEL_DISEMBARK` actions — **flight owns the `embark`/`disembark` verbs** and falls through to them when there's no aircraft. Reads: `waterTemperature` (env), extends **clothing-wetness** (submersion) + the `gameLoop` temp tick (submerged → water temp + soaked). See [systems-swimming.md](systems-swimming.md) |
| **sanity** | **The slide into madness** — the *consequence* of low `player.sanity` (nothing here drains it; drugs/sleep own that). A gradual, scarier-than-a-drug-trip dread over one continuous intensity curve: **creep** (25–49) cold vignette + dread bed + misperception whispers; **halluc** (<25) fake people/animals walk into the real room; **insane** (≤0) full breakdown | — | `tick.minute` walks `world.players` off live `player.sanity`; pushes the `sanity_fx` client channel (its own overlay so it composes with a drug trip) + `.msg-dread` whisper lines. **Hallucinations reuse the shared per-player phantom registry** ([phantoms.js](../server/engine/phantoms.js)) — the **trip** plugin's look/attack/talk specialized actions answer them for free (`matchPhantom` is global); only removes its OWN phantoms (`sane_*`), never `clearPhantoms`. **Insane** sets `player.insane`; the engine's substrate gate in [commands/index.js](../server/engine/commands/index.js) scrambles ~55% of non-sleep commands (mirrors the `blackedOutUntil` gate), lifting with hysteresis at sanity 10 (`sleep`/`rest` always pass — the only way out). See [systems-survival.md](systems-survival.md)  **Voices ([voices.js](../plugins/sanity/voices.js)) are emitted through the EXACT wire format real speech uses** — byte-identical to `formatChitchat` / `cmdSay`, inline style not a class, no speaker id — so a hallucinated line is indistinguishable from a real one and the only tell is out of band (the speaker is not in the room pane). Ladder: misattributed speech from someone genuinely present (42) → phantoms (25) → disembodied voices from someone provably elsewhere (18) → the room itself warps (12), each rung harder to verify than the last. |
| **mining** | Perpetual, posture-based deposit-working (scavenging's rock-face cousin) — per-zone ore tables, the 2d8−2d8 Mining check, lazy weighted replenish; adds a tool gate | `mine` | Sets `player.posture="mining"` + runtime `mineState`; engine reads it in `describePlayerAppearance` and clears it via the usual force-stand triggers. 1s tick drives 3.8s swings. Pick carry-gate (`mining_tool` tag). Zone opt-in via `flags.mining_table_id`; reuses the scavenging table schema. See [systems-mining.md](systems-mining.md) |
| **work** | **Steady Work** — tier-two employment above the job board, **ungated**. **Two archetypes.** *Shift:* `clock in` at a venue, ride a posture-tick of timed skill-check events (order-up/check/fryer/regular/drunk + one mid-shift **rush**), keep the manager's hidden satisfaction up or get **sent home** with cut pay; wage flat, tips scale off satisfaction. *Courier:* carry a real, keepable **parcel** across the map before a deadline — clean/sketchy on the board (or Tablet **Steady Work** tab), **hot** offered by a fence in dialogue; `crack` the seal to steal the contents (irreversible — burns the run) | `work`/`shift`/`shifts`, `clock` (`clock in`/`out`), shift verbs `serve` `bill` `douse` `soothe` `bounce`; `courier`/`runs` (list board / `courier <n>` to take), `deliver`, `crack` | **Shift:** sets `player.posture="working"` + runtime `shiftState`; own 1s tick, ends free on any force-stand via `posture.changed`/`player.stop`. Second venue: Voltage (`flags.work_venue.pool='bar'`, Brawn/Cool-leaning). **Third: Watts's repair bench** (`pool='bench'`, Brains-leaning — hand back a finished ticket, quote a job that costs more than the thing is worth, smother a solvent flare, let down someone whose mother's coat can't be saved, refuse a rig with the serial ground off). It reuses the same five verbs, so no new global verb; and finishing a shift **raises your standing with the employer NPC** (`employer_npc`), which lowers what he charges to fix YOUR gear — the durability loop paying itself off. **Courier:** the run IS a real `player_inventory` item — its `custom_data` holds `{courier,class,dropoffZone,deadlineAt,payout,sealed}` (persistent, self-cleaning, no quest row/player field). Sketchy/hot parcels carry the **`contraband`** tag, so the existing jail confiscation / `conceal` / gov-checkpoint stack handles a search for free. Hot runs come from the `OFFER_COURIER_HOT` action on a fence's dialogue branch (Voss the salvage-broker), gated on the `work_fence_blacklist` flag being unset; cracking a hot run blacklists you (+ a `WANTED_RAISE` bounty when fenced value ≥ `work_fence_bounty_payout_mult`× payout). **No XP gate** — discovery, not permission, is the on-ramp: bare `work` lists every venue going. The fence's hot runs keep their own trust gate (`work_fence_blacklist`). Tunables `work_shift_*`/`work_event_chance`/`work_courier_*`/`work_fence_bounty_payout_mult`. Pays via `adjustCredits` |
| **broadcast** | **SHARED VERB `listen`** — with a radio/screen in the room it means "what is on"; anywhere else it falls through to the engine sense (`cmdListen`, room/adjacent-zone hearing). `watch` deliberately does NOT fall through. Media framework — scripted channels, dynamic news, VINE graph scripts, camera feeds, NPC hosts; TV popup presentation layer; media-deck **piracy** (firmware-gated station takeover) | `tune watch listen tv load eject uplink selectcassette patch pirate pirateresolve air airemergency endemergency tablettune guess` | USE specialized actions (tags `broadcast_receiver`, `media_deck`, `piracy_firmware`); broadcast tick every 1 s (`BROADCAST_TICK_MS`); event consumers (`player.death`, `flag.set`, `npc.broadcast_say`); exposes `hasChannelViewers` via broadcast-bridge for AI conditions; `tv`/`watch tv` opens in-page TV panel. **Portable tablet TV:** the Tablet OS `tv` app (`plugins/tablet/tv-app.js`) streams any channel with **no `broadcast_receiver` device in the zone** — `tablettune <n>` resolves the channel straight from the in-memory runtime and registers a per-player tuner (`tabletTuners`, kept separate from `tvWatchers` so the wall set and the tablet can sit on different channels at once), and a dedicated pass at the end of `broadcastTick` delivers to it, REUSING the beat another pass already resolved so the stateful VINE walker never double-advances. Client-side both surfaces are driven by ONE renderer (`createTvView` in `client/game/js/panels/tv.js`), so the tablet app inherits every TV feature. **Piracy:** `use <pirate firmware>` flashes the `piracy_installed` player flag; `pirate <deck>` launches the client **Signal Hijack** minigame (`signal_hijack` push → `signalhijack.js`); `pirateresolve` seizes on win — sets `pirate_owner`/`pirate_since` + seeds `pirate_queue` on the deck's furniture flags, charges `broadcast_piracy` via the `CHARGE_CRIME` action, fires a tamper ping. Deck-operate handlers (`use`/`load`/`eject`/`selectcassette`) gate on `canOperateDeck` (admin/dev or the current pirate); the schedule-sync tick skips pirated decks. **Pirate console (`air`):** the captor's `use <deck>` (or `air` from anywhere) opens a self-mounted console (`pirate_console` push → `piratedeck.js`); `air play\|stop\|skip\|loop <mode>\|recorded\|live [src]\|source <src>\|add <name>\|remove <n>\|move <n> <m>\|crawl <text\|off>\|close` edits the `pirate_*` deck flags; `_getPirateMessage` runs the pirate output in `_getDeckMessage`, overriding both the channel schedule and `deck_active`. **RECORDED** mode plays the `pirate_queue` (real-duration auto-advance via `broadcastDuration`, loop off/item/queue). **LIVE** mode (`pirate_mode='live'`) cuts to a camera via `buildCameraSnapshot(zoneId)` — the station studio cam or any SPECTER cam the captor controls (read-only from `security_devices`, no surveillance import); the ticker crawl rides both modes. **Reclaim (the station fights back):** a 15 s `engineerTick` reboots any deck whose 2-min defend window elapsed (`_engineerDueAt` on `pirate_since`/`pirate_engineer_at`) **unless** the captor is standing at the deck (runs the engineer off, retries later); `player.death` releases every seizure the victim held (`_releaseSeizuresBy`); counter-hack works because `cmdPirate` lets a non-owner re-run Signal Hijack. All clear via `_clearSeizure` (wipes `pirate_*` → channel programming resumes). **Game shows (`guess`):** the `gameshow` broadcast type ([gameshow.js](../plugins/broadcast/gameshow.js), a sibling module) is the only show a player can be *inside* — stand in a channel's `studio_zone_id` while a round is open and `guess <number\|higher\|lower\|2 1 3>` enters you as a contestant. Questions are dealt from `items.value` through the boot-loaded item cache (**zero queries**); `furniture.price` is deliberately unused (per-instance rows, half unpriced, uncached). The handler validates then `sendToZone`s the guess as speech, so the **existing** studio relay (`zone.broadcast`) televises it citywide — no new WS message, no client code. Round control is two *instantaneous* nodes (`gameshow_round`/`gameshow_reveal`); the guess window is the host's patter between them, and the outcome reaches air via the `{winner}`/`{verdict}`/`{guesses}` tokens merged into `_scriptedTokens` (not by branching — the `FLAG_SET` broadcast condition is a stub returning `false`). `gameshowResolveRound` is idempotent so it can never double-pay. Wins mint credits (40₵/round, 250₵ + the actual lot on the Showcase) gated by `player_flags.gameshow_win_cooldown` — epoch-ms of the last paid win, 6-hour window, checked at *payout* time with an on-air `no_payout` line. Contestant NPCs are name STRINGS, never spawned bodies. See [bsm-format.md](bsm-format.md#game-shows-type-gameshow)  **Small-format players** (`flags.mini_deck`) are ordinary media decks scoped to one room and one set — the TV never "tunes to" them, the deck substitutes what that channel shows for that zone only (shared `channel_id`); `_playDeckItem` already loops, so playback needed no new code. Its ONE engine hook is `tick.minute`: a deck naming `flags.deck_owner_npc` + `flags.deck_default` gets put back on by that NPC when someone changes it, scoped to zones containing a player. **The spare input (`patch`):** a consumer deck can take one of your own SPECTER sticky cams as its input instead of a cassette (`flags.deck_cam_source`), putting a live feed on the set in that room only — `_getDeckMessage` checks it before the tape path, frames come from surveillance's cache-backed `camPatchFrame`/`camSourcesFor` (no query per tick), and the tablet SPECTER app patches via the exported `miniDeckHere`/`patchCamToDeck`. `canOperateDeck` now passes any `mini_deck` — an appliance has no frequency to seize. **The portable mediadeck (`uplink`):** a shoot away from the channel's own studio needs a `portable_mediadeck` in the room to get its signal home, and that case is now a transmitter in its own right — a bound, powered, switched-on portable answers for the whole channel (`channelTransmitterLive`), not only for the location shot. `uplink` reports its state, `uplink on`/`uplink off` writes `flags.deck_off`; deliberately NOT owner-gated, because killing a broadcast by walking up to the case is the interference the on-location model exists for. ⚠ Both power tests here were dead until 2026-08-31 — `_portableDeckLive` read `furniture.is_powered` (a column that only exists on `media_cameras`/`security_devices`, so `undefined !== 0` was always true) and `channelTransmitterLive` read `getZone(...).powerStatus` (never written on a world zone object). Both now ask `getZonePowerStatus`, and only an explicit `offline` kills a feed, so an unmodelled room still transmits |
| **pinch** | Wake an offline-sleeping player and path them home; `home` binds your HoloLock (in an apartment you own) or auto-walks you home then sleeps you | `pinch home` | `pinch` targets offline_sleeping players with a `home_zone` not in that home — clears offline_sleeping, starts BFS walk (1 zone/min via `tick.minute` hook). `home` in an apartment you own calls `cmdSetHome` (binds `player.home_zone`); anywhere else it sets `player.goingHome=true` and the same hook steps you via `cmdMove {bypassEncumbrance}`, arriving to sleep at home rest rates. Type `home` again while walking to cancel. (Both jobs sit on the one verb: the dispatcher strips a leading dot before matching, so `.home`/`.gohome` are the same key.) |
| **slots** | Single-line slot machines (The Lucky Bastard casino). `spin [bet]` targets the first `flags.slot_machine` furniture in the room, three weighted reels, ~0.75 RTP, ASCII cabinet + jackpot `zone_event`. See [systems-casino.md](systems-casino.md) | `spin slots` | Reads `slot_machine`/`slot_min`/`slot_max`/`slot_default` furniture flags; `adjustCredits` for bet/payout. No hooks |
| **gametable** | Multiplayer table games: poker (Texas Hold'em) on the felt and **chess** on the board, sharing one seat/spectator/persistence layer (`TableBase`) | `join seat leave spectate watch look say help check call deal summon calldealer bet raise fold allin table board pot players showhand evict pokertext text visual move chessmove chesspick threats resign offerdraw acceptdraw declinedraw` | Owns the `game_tables` table (JSONB state, persisted every 10 s + hand end). **Verb intercepts while seated:** bare `look` pushes the poker pane instead of room HTML; `say` floats a table speech bubble then falls through; bare `help` shows poker help; `raise` falls through to the engine spend-IP verb when unseated. **`watch` router** (manifest `after:["broadcast","interactions"]` so it wins the verb): poker rooms spectate, TV rooms delegate to `broadcast.commands.watch`, both → SIFT (replay via the `gametable.watch_choice` Action), neither → `interactions.commands.watch` furniture emote. **Cross-plugin seat bridge:** exposes the `gametable.take_seat` Action so interactions' `sit` can seat players on poker chairs. Implements `zone.furniturePanel` (collapses the four chairs into one **Poker:** panel with click-to-sit seats). **Hands require a living dealer NPC in the room** (`GameTable.hasDealer()`/`_dealerNpc()` — explicit `config.dealerNpcId`, else an NPC flagged `flags.table_id` for that table); without one the table sits in a `WaitingForDealer` phase, seated players are notified once, and the pane shows a highlighted **call dealer** button. `summon`/`deal [in] <name>` calls a `poker_player`-flagged gambler NPC to an open seat (bare form auto-picks whoever's free — also a one-click **call AI** button); `call dealer`/`calldealer` rushes the table's *own* assigned dealer back from wherever he wandered (2 zones/tick, faster than a gambler's normal walk-in) — resolved world-wide by `dealerNpcId`/`flags.table_id` only, never the generic `npc_type==='dealer'` fallback (that one's zone-local flavor-only and could otherwise snag an unrelated dealer-typed NPC, e.g. the covert drugs dealer). Dealer narration/banter, denominated ₵ bet animations, and procedural sound cues (`poker_sfx` → `client/game/js/poker-sfx.js`). **`evict <gambler>`** clears a bot out of a seat (falls through to the engine's admin housing `evict` when you're not at a table). **`pokertext [on\|off]`** (aliases `text`/`visual`) is a per-player screen-reader opt-in ([text-mode.js](../plugins/gametable/text-mode.js)): narrates hole cards / board / your-turn to the scrolling log in ASCII on top of the visual pane, persisted as the game-wide **Display Mode** (`player_flags.display_mode`, shared with the flight display — see [server/engine/presentation.js](../server/engine/presentation.js) and the tablet's `displaymode` verb; runtime `Set`, no hot-path DB read). **Chess** rides the same plugin: `game_tables.game_type` (stored but never read before) now picks the class via `tables.js`, `ChessTable` is 2-seat with no dealer requirement and an optional `config.stake`, and the board renders as an isometric pane (`table_update`) whose squares are `.poker-cmd` verbs. `move` is claimed here but **falls through to the engine's movement verb** unless the input is a legal move at a board you're seated at. Detail in `plugins/gametable/`, [systems-casino.md](systems-casino.md) and [systems-chess.md](systems-chess.md) |

| **mis** | Mature Interaction System — opt-in sexual verbs, horniness/arousal, ongoing events, climax, `wash`, `strip` (direct bare, no club gates), MIS-gated appearance notes | `mis touch grope squeeze kiss lick fondle slap stroke masturbate jerkoff jackoff rubself fingerself finger suck fuck sex screw rail bang breed blowjob bj handjob hj ejaculate cum come wash strip` — plus `consent`/`revoke`, the player half of the two-switch opt-in gate — + input matchers `jerk off on` / `eat out` | The engine keeps only the **consent substrate** (`server/engine/mis.js`: `isMisActive`/`isAttractedTo` + server setting) — read by bodily, appearance rendering, MORPHEX. Plugin implements the `player.appearanceMisNotes` hook (describePlayerAppearance fires it at its naked/clothed sites), consumes `player.stop` (halts ongoing events) and `mis.toggled` (WS Maturity-Slider toggle → tutorial / event cleanup), and runs horniness decay on a `1m` tick. **`strip <target>`** skips the strip-club tip/heat/willing/zone gates: an NPC is bared freely (`_forcedNude`/`_clothingPeeled`, honored by the strippers redress tick); a target *player* only if they also have MIS enabled (their opt-in = consent), unequipping all worn garments except the weapon into their pack. State module: `plugins/mis/mis-system.js`. See [systems-mis.md](systems-mis.md) + [plugins/mis/README.md](../plugins/mis/README.md) |
| **topical** | **Any liquid landing on a body** — the player-facing half of the topical-contact substrate (`server/engine/topical.js`): throw what you're carrying over somebody, plus the opt-in switch deciding whether another PLAYER may do it to you | `splash <target> [with <container>]` · `sprayconsent` (`on`/`off`; bare = your current answer) | Three rules, all in the substrate. **(1) The law:** every liquid wets you, so the wetting pass is registered ONCE (`registerTopicalWetting`, claimed by **clothing-wetness**, which owns wetness) and runs for beer and fuel exactly as for water, through the same outside-in layer walk as rain — a slicker is worth as much against a thrown pint as against a downpour. **(2) The registries:** `registerTopicalEffect(fluid, fn)` claims a consequence, `registerFluidResolver(fn)` claims a CONTAINER (fillable answers for `fluid_amount`, drinks for `custom_data.drink`), so nothing learns anyone else's schema and an unclaimed liquid still lands + wets rather than being silently inert. **(3) The gate**, checked inside `applyTopical` not at each call site: `actor && actor.id !== target.id` — weather, NPCs and your own bucket land regardless, because nobody consents to rain. **An OPT OUT, on by default** (default-off would make every liquid inert for anyone who never opened a settings page, which reads as broken), and a VERB with no tablet surface (a switch you may want in a hurry shouldn't be four taps inside an app); the flag is written only when you change it. A refusal names nobody. Fluids: water (rinses sweat, cools an overheated body) · dirty_water · booze · soft_drink · hot_drink (scalds, never cools) · fuel · acid (eats every worn garment + burns) · solvent · blood · chem — each leaving a hygiene contaminant and a floor stain. **ABSORPTION is the second axis** — `dose = potency × absorb × skinExposure` floored at `MIN_SYSTEMIC_DOSE` (0.15), where `absorb` is the liquid's permeability (alcohol 0.02 → a full pint on a bare chest doses NOTHING; solvent 0.85) and `skinExposure` is what got past the clothes, which the wetting pass already computed and used to discard — **so a raincoat is chemical protection for free**. What's dissolved in a liquid rides on the CONTAINER, never the fluid table (`fillable` reads `custom_data.drug_id`, `drinks` names `drug_alcohol`), and a real dose goes through the ordinary `useDrug` on a new **`skin` route** (onset ×4, intensity ×0.7). A harmful fluid or any splash that got INTO somebody charges `assault_chemical` (3★, forced-witnessed — the victim was standing there); a drink in the face charges nothing. **A splash floors at 1 HP and can never kill** (killing owns a death path this verb shouldn't). Not named `douse` — **work** owns that verb. `player_flags.topical_consent`; no new column, no table. Emits `topical.applied`. Callers: `splash`, flight's `spray`. See [plugins/topical/README.md](../plugins/topical/README.md) |
| **thievery** | Pocket-picking live players | `steal` | Deception check vs. 7; 60 s cooldown persisted in the Flag store (`steal_cooldown_until`) so it survives restarts; SIFT ambiguous picks replay via the `thievery.steal` Action |
| **laundry** | **Coin laundry** — the other hygiene clock. A shower cleans the body; this cleans what you put back on, and it is the ONLY thing that resets `hygiene_laundered_at` ([systems-hygiene.md](systems-hygiene.md)) | `launder` / `laundry` | Tag-gated specialized action on furniture flagged `washing_machine`, so a machine advertises LAUNDER on examine; the handler returns undefined with no machine present so it never shadows anything. Price (`wash_price`, default 12₵) and cycle length (`wash_cycle_ms`) are read off the furniture, so a coin laundry and a penthouse machine differ with no code. Charges at the END of the cycle — walk out mid-wash and you lose the clothes-cleaning, not the money. Dead grid = dead machine (`getZonePowerInfo`). No tick, no table. **A machine is a place, not a capability**: The Wash is four numbered machines, a running one is CLAIMED for its cycle (runtime `busy` map, keyed by furniture id), and the room says so through the engine's `zone.furnitureOccupants` seam rather than any prose of this plugin's own — the same line that says a booth is taken. ⚠ A named machine and a named CATEGORY are told apart by match count: `launder three` hits one row and means that drum, `launder washer` hits four and means "a free one". **And you have to take the clothes off** — the gate is `undress`'s own five body slots, so accessories stay on, and you spend the two minutes standing there in your underwear |
| **bodily** | Bowel/bladder pressure, involuntary release, relief verbs, toilet/sink/`shower` `use` panels | `pee urinate piss poop defecate shit fart flush shower soap` | 1m tick (skips sleeping players, mirroring the old engine tick); self-gated `use` specialized action for toilet/sink/shower panels; SIFT on-player picks replay via `bodily.pee_target`/`bodily.poop_target` Actions. **`shower`** is gated on a shower fixture in the zone (`object_type='shower'`/`flags.shower`/name match) and plays as a **~15s timed ritual** (`showerSessions` map + staged `setTimeout` beats, same pattern as the toilet routine): three descriptive beats, and only the final beat does the thorough clean — clears `clothing_contamination`, bare-skin `soiled_state`, dried `ejaculate_state`, and `covered_in_blood` (a superset of MIS `wash`) + a brief cosmetic `refreshed` badge (~180s). Walking out of the zone (or disconnecting) aborts the ritual with no clean; a second `shower` while one is running is refused. **Stains and digestion loads stay engine** (`server/engine/bodily.js` substrate: `stainClothing`/`stainZone`/`foodLoad`/`drinkLoad`/`applyThirst` — written/read by mis, butchering, drugs, water, fillable, inventory) |
| **commerce** | Vendor shopping verbs + balance display + **self-service checkout** + the **unpaid-goods door prompt** | `shop browse buy sell balance checkout yes no` | **The door asks first:** a `commerce:unpaid-door` move gate stops you ONCE on the way out while you're still carrying unpaid goods, naming them and the total, and `yes`/`no` answer it — `yes` settles at the counter (and if you're short, puts the goods back on the shelf) then walks you out, `no` walks you out and lets the theft charge. Warn once, not a wall: taking the same step again is `no`. The prompt charges nothing (the crime still fires on the committed step) and is shared — `commerce.arm_door_prompt` / `commerce.clear_door_prompt` plus a caller-supplied settle action let another shop door (storefront) use the same yes/no. Because you are asked plainly and walk anyway, `shoplifting` is **3 stars**. **Self-service:** a furniture container flagged `vendor_stock: <npcId>` is a shop-floor display case — pulling from it marks the row `custom_data.unpaid` (engine, commands/inventory.js; `unpaid` is an INSTANCE_KEY so it can't launder itself into a paid stack), putting it back clears the mark, `checkout` settles the lot at a `checkout: <npcId>`-flagged counter — or, in a room with no such counter, with a vendor standing there who is owed something — at catalogue price + ideology discount (firing the same `vendor.purchase` seam a shelf buy does), and carrying it out of the shop building emits `shoplifting.caught` → surveillance charges the `shoplifting` crime **at the shop's zone** (the clerk and the shop camera are the witnesses) plus a vendor grudge. Leaving is detected on `zone.entered` (a committed step, not a move gate) against an in-memory shop-zone index, so a normal move costs no query. Otherwise a thin verb layer over the engine vendor services (vendor.js pricing/trust/stock, furniture-shop.js, vendor-session.js — all stay engine); SIFT picks replay via `commerce.shop_vendor`/`commerce.buy_item` Actions. Also listens for `hololock.breached` to hold a **vendor grudge** (engine `vendor-grudge.js`) against anyone who burgles an apartment an NPC vendor **owns** (doors.js attributes the break-in to the apartment's `owner_id`/`owner_type`, so it sticks whether or not the owner was home) — grudged vendors refuse to open/buy/sell for 7 in-game days (also set by vendor-safe on a safe hack; enforced at every trade funnel incl. the dialogue `OPEN_SHOP`) |
| **cleaning** | **Mopping your own floor** — the room half of hygiene ([systems-cleaning.md](systems-cleaning.md)). Owned rooms no longer get the nightly sweep (they keep stains for a full rent cycle via `server/engine/zone-filth.js`), so somebody has to do the work | `clean` / `mop` | Leaf: no table, no tick, no skill. Stains are RAM-authoritative on the zone, so a clean is a synchronous Map mutation. A `cleaning_tool` (carried **or** furniture in the room) clears the whole floor; bare hands clear one patch and leave the filth on you — a tool is *rewarded*, not required, because requiring one means nobody ever cleans. Costs sweat through the hygiene substrate, so a scrub earns you a shower. A clean floor answers instead of erroring, so `clean` never falls through to `Unknown command` |
| **graffiti** | **Spraying a tag on a building front.** The `graffiti` crime sat in `crimes.js` at 0.3★ charging nobody since the crime system was built, because there was no verb — this is the verb | `tag <direction\|building> <text>`, `spraycan [wall]` (removal is cleaning's `clean`); `sprayapply spraysave spraydel` are panel-fired, not typed | Three rules carry it. **You spray a BUILDING, not a tile:** it resolves a facade on an adjacent exit (`flags.is_building`) and refuses on open ground, so a tag always lands on a thing that exists and the room line names it. **One per street tile, anyone may paint over anyone** — the wall is a contested slot, which is what makes the cap a `zone_graffiti` PRIMARY KEY (one row per tile in the world, ever, upserted) rather than a rule somebody enforces; keyed on the tile you STAND in, not the facade, because that's where it's read from. **It ages out after 3 game days**, derived from the game DATE (`gameDayIndex`, the `zone-filth.js` trick) — stateless, no tick, no column to reset, and expiry is lazy and fails toward *"the tag is still there"*. RAM-authoritative: hydrates once, so the room-description path never queries; this plugin is the only writer, which is what makes that cache safe. Player text is **escaped on the way IN** and stored escaped — it lands in a stranger's HTML. `clean` takes a tag off but **needs a real `cleaning_tool`** — bare hands do floor filth, not brickwork, and that asymmetry is the teeth. **`spraycan` is the same act with the lid off:** an in-browser dialog with per-letter colour off the SHARED wheel (`client/game/js/panels/color-picker.js`, the one the hangar paint bench uses), bold/italic/underline/strike, and a shelf of saved designs (`player_sprays`, cap 12). It weakens none of the above because **style is DATA, never markup** — colour and weight ride alongside the still-escaped text as validated runs `[{n,c,f}]` (`paint.js`), so a room description has no markup to parse and none to get wrong; the run indices count TYPED characters and the renderer splits the escaped string entity-by-entity to realign them. The verb is `spraycan` and not `spray` because **flight already owns `spray`** (the Locust crop-duster boom) and plugins do not beat each other. `tag <dir>` with no words and a can in hand opens the dialog, which is the discovery path. **Paint is a budget, not a charge**: a can holds 120 characters (`CAN_CAPACITY`) spent letter by letter, tracked on the inventory row's `custom_data.paint`, so a half-used can survives being dropped, traded and picked up; a stack's remainder describes the can in hand and the next starts full. At ₵120 a can that is a credit a letter. Emits `graffiti.tagged` → surveillance charges it through the ordinary witness gate |
| **videostore** | **Renting a tape off a wall, and being chased for it** (see [plugins/videostore/README.md](../plugins/videostore/README.md)). The tapes already worked — a `media_cassette` item carrying a `broadcast_id` has loaded into any deck since broadcast shipped — but nothing ever gave a tape a reason to come BACK, which is the whole difference between a video shop and a vendor with a theme | `rentals`, `borrow <title>`, `returntape [title]`, `settle` | All four gated on furniture `flags.tape_rental`, stocked from the authored `flags.tape_shelf` list. **Stock is finite and shared** — a copy that is out is a copy nobody else can have, and that is the only scarcity the system has. **Nothing ticks and nothing accrues:** the late fee is derived on read from `today - due_day`, both game-day indices (`gameDayIndex`, the corp-rackets `fearNow` pattern), so an offline week owes nobody a catch-up pass; past `WRITE_OFF_DAYS` the accrual STOPS and the print is billed instead, so a lost tape has a finite price. A debt closes the WALL and nothing else — no wanted level, no hostility, no lockout from the rest of the shop. The borrowed tape is an **ordinary inventory row** with no rental marker, so it drops/trades/gets stolen normally while the rental row survives: the shop lent you the TITLE, not the row. `returntape` rather than `return` so a control-flow-shaped word stays free. One table (`tape_rentals`), written only on borrow/return/settle |
| **library** | **The books, and the two ways to read them** ([systems-library.md](systems-library.md)). They live in `books` (readTier cold, never boot-loaded) and are read by the tablet's LIBRARY app AND by this plugin's typed reader. Both call `plugins/library/books.js` and share the `book_pos_<id>` bookmark, so a book put down in one is picked up in the other. **The typed reader is why the shelf is reachable at all without a tablet** — until it existed `plugins/library/index.js` exported no commands and the entire content set was tap-only. Its one addition is PAGINATION (`book_page_<id>`, the reader's alone): a novel chapter pushed to the log in one go is thousands of words nobody can navigate, so `page` serves ~1400 chars broken at a paragraph, never mid-sentence. **Two shelves:** `books.kind` splits the literature shelf from the **longbox** (the four in-universe comics), which is a screen of its own on the tablet and a second listing (`longbox`/`comics`) when typed. The split is presentation ONLY — `read <title>` matches across both, so nobody has to know a title is filed as a comic to open it. A comic's chapter is prose plus four paragraph markers (caption / balloon / lettering / page turn) parsed in exactly one place (`comicBlocks`), because the tablet draws furniture from them, the typed reader must show none of them, and Read Aloud must speak the words and none of the marks | `library books longbox comics page chapter contents` + `scan`/`read` (specialized actions) | Sets the `library_unlocked` player flag — the sole thing gating BOTH front ends — before that the app is **not on the home screen at all** and every verb here refuses (`read` silently falls through, so a locked player never learns the shelf exists). First scan prints an intro covering narration, minimize-and-keep-reading, the tap-to-gloss layer and per-book bookmarks; later scans are a gentle no-op. `furniture.describe` teaches the verb once via the `teachVerb` shimmer. Leaf: no table, no tick |
| **injury** | **Wounds that outlive the fight** ([proposals/injury-system.md](proposals/injury-system.md)) — one injury per body part (the seven `combat.js` already rolls, arms/legs lateralized), three severities, named by the damage type that caused them. **All five phases built plus §8b: wounds appear, PENALISE, heal on their own, and can be treated** — field kits step a wound down but can never clear it (they floor at Bruised); only a clinic makes you whole. The PART owns what a wound does; the TYPE owns how likely/bad/long and what it reads like, so it stays two lists of 7 and 5 rather than a 35-cell matrix. Blunt is the interesting row: a high bar most hits fail, then a steep uncapped climb — a pipe glances off your ribs eleven times and the twelfth breaks them | `injuries`, `aim` | Consumes the `server/engine/damage-events.js` substrate (`registerDamageObserver`, fired at `combat.js:549`/`:646`/`:720`) — **sync and query-free by contract**, it runs on every swing. **Zero runtime queries:** state parses on first touch out of the already-hydrated `player._flags` `injuries` key and flushes coalesced on `tick.minute`. Decay is **lazy with no tick of its own** (the `player_npc_relations` pattern) — nothing heals until someone reads it, so a restart can't reset anyone's wounds and offline healing is free; sleeping heals faster by *backdating the stamp*, not via a second decay path. `tables.js` is the entire tunable surface. Penalties publish through the **`server/engine/impairment.js`** substrate (stat penalties, stamina regen, to-hit, run refusal, per-step stamina) — **nothing ever blocks movement outright**, by design. Medicine rides the `treat_injury` item tag via the `item.consumed` hook; `plugins/clinic` calls `clearInjuries` for the surgical tier. Exports `severityOf`/`injuryReport`/`bodyReport`/`clearInjuries` (`bodyReport` drives the Vitals paper doll in `plugins/tablet/health-app.js`). **`aim <part>`** is the opt-in half: it BIASES `rollBodyPart`'s existing weights (no second targeting path) at a to-hit cost that weapon skill partly buys back, via the plain `player._aimPart` field the engine reads. **Enemies are wounded too** (`enemy.js`, §8b) — in-memory on the instance so it dies with the mob: zero storage, zero decay, zero naming. Wounded arms cost a mob to-hit, wounded legs cost it the flee roll, both read as plain `_injuryHitMod`/`_injuryFleeMod` fields. Curve retuned 2026-07-29 (geometric rungs + the `baseDamage` no-double-dip contract) after simulation found three weapons maiming on 100% of hits through heavy armour. No table of its own |
| **clinic** | **Paid treatment at a medic** — the service half of a clinic (the medics already *sell* supplies through an ordinary vendor `__shop__` node) | — | One Action, `CLINIC_TREAT`, no state/tables/ticks. Quotes off what's actually wrong with you — `max(minimum, ceil(missingHP × rate)) + (bleeding ? bleed_fee : 0)`, all four params (+`free`) read **flat** off the dialogue node so a back-alley cutter and a corporate trauma bay charge different money off the same Action. Debits via the engine `adjustCredits` guarded UPDATE (no window where the money moves but the healing doesn't), then restores HP to `hp_max`, clears `bleeding`, and discards pending `healOverTime` (a paid patch-up supersedes the kit you'd already cracked on yourself). Persisted by the existing gameLoop save — no DB write of its own. **Not-injured is a free flavour line, not a bill** — `renderDialogueNode` fires node actions on every draw, so this is what makes re-rendering safe. Wired on Sister Ida Adler (`npc_clinic_medic`, 2/10/25) and Dr. Priya Anand (`npc_medic_anand`, 3/15/30). No verb: `heal` belongs to dev-tools as an admin cheat and is deliberately not shadowed. See [plugins/clinic/README.md](../plugins/clinic/README.md) |
| **wear** | **Repairing worn gear** — field patch-ups and proper bench work over the engine durability substrate ([systems-durability.md](systems-durability.md)) | `repair` (specialized action, **no requiredTag** — what's repairable is *derived* from the item, never authored) | Registered as a specialized action rather than a command precisely so `repair` is **shared, not owned**: flight's `repair` (a plugin command) runs first and self-gates on "am I at a hangar with a craft"; the engine builtin targets infrastructure; this answers for carried gear from a different registry, so there's no collision to declare. Ambiguous picks replay through the **`wear.repair_item`** Action (the SIFT replay trap). **Field** repair needs a `repair_kit` item, works anywhere, and caps at Battered — you can never make a thing right in the mud; **bench** repair needs an NPC flagged `repairman`, restores fully, and is priced `max(8, value × 0.4 × missing)` **discounted by `relationHelp`** — your repairman charges you less. Difficulty scales with item value. Outcomes are margin-driven (`rough`/`sound`/`masterful`); a failed repair costs 5% progress, which can finish off something already Failing (items disintegrate at zero — there is no broken state). **Reinforcement** — a masterful HAND repair at Fabrication 6+ — makes an item permanently tougher AND forgives its accumulated repair-fatigue, which a bench can never do; that is the whole reason to seek out a skilled player. Every repair increments `custom_data.repairs`, surfaced on examine ("battered, twice-mended"). See [plugins/wear/README.md](../plugins/wear/README.md) |
| **emergency** | Emergency Security Protocol — zone-wide siren, warning broadcasts, red-alert UI | — | hooks `furniture.describe` + `player.death`; routes at `/emergency` |
| **demolition** | **Breaching charges** ([systems-demolition.md](systems-demolition.md)). The rule the whole thing turns on: **a charge is not a weapon** — there is no verb that throws one at a person. You wire it to a THING, choose how long you have, and the interesting part is that you are standing next to it. So the fuse is public (the room is told it has started ticking), the charge is defusable by anybody who walks in, and the blast does not care whose it was. **Both boards run on all three Display Mode rungs**, and the middle one is the same game rather than a described one, because `demolitiongame.js` owns the loop and the two panels are skins over it | `breach <thing> [with <charge>]`, `defuse [<thing>]`, `charges`; `breachresolve`/`defuseresolve` are board-fired, never typed | Owns almost nothing: damage is `applyStrikeToPlayer` (never `player.hp -=`, so injury's observers and typed soak both apply), sound is `propagateSound`, the crime is the `CHARGE_CRIME` action dispatched by name (so this plugin never imports surveillance) — `arson` when somebody was in the room, `vandalism` when nobody was, which is the distinction the crime registry itself draws — and quest progress is one `demolition.detonated` subscriber over in `plugins/quests`, giving the `demolish` objective type for free. Live charges are **RAM-only, keyed by id, swept by one `1s` subscriber**; a restart forgets them and the durable residue is the `destroyed` flag on the furniture row, so there is no table. ⚠ `breachresolve` is the only resolve verb that carries a NUMBER back (the chosen fuse) — the server clamps it, because a client is never trusted with how long anybody has to react. Authoring is two tags: `demolishable` on the furniture, `explosive_charge` on the item |
| **knock** | Knock on a door in a direction; heard only in the adjacent same-floor room | `knock` | — |
| **sneak** | **Sneaking, and knocking people out** ([systems-stealth.md](systems-stealth.md)). The rule that shapes it: **combat is to the death and stays that way** — a random KO mid-fight would be invisible (auto-attack finishes an unconscious body a tick later), so a knockout is *always* something somebody chose | `sneak knockout` | Stealth is **borrowed, not built**: the notice roll is assembled from `senses`, `getZoneVisibility`, `impairment` and `posture`, and is **per observer**, so one person can miss you while another doesn't. Consumes `zone.entered`, `stealth.window`, `posture.changed`, `player.logout`. `knockout` needs the sneaking posture and an unaware target; a failure makes an NPC bolt and shout and an enemy turn on you, and a success in front of witnesses does both. An out-cold body is **killable where it lies**, which is why finishing one is charged as `execution` at 5★ while the cosh itself is a 2★ camera-only crime |
| **preservation** | Lazy freshness/decay for perishable food — **no global tick**, recomputed on examine/stow/pull/eat | `preserve` | `item.checkFreshness` hook; timestamp-based, so cost is paid only when somebody looks. `preserve` spends a vial of BHT to slow one item's decay rate **wherever it sits** (no appliance needed) |
| **appliances** | Generic `flags.plugged_in` gate for **any** powered furniture (anything with `power_draw_kw`) | `unplug` | `furniture.describe` hook adds a shared "looks broken" line when unplugged, so a dead appliance reads as dead without per-item authoring. **An absent flag means plugged in**, for backward compatibility with everything authored before this existed. The inverse `plug <name>` is not here — it arrives via the **generator** plugin's fallback to `togglePluggedByName()`. Consumed by vending and preservation |
| **idle-logoff** | Warns players at 15 min without deliberate input, disconnects at 20 (same `kicked` path as an admin kick). Everyone — no role or combat exemptions | — | `tick.minute` sweep over live players; reads `player._lastInputAt` (runtime-only, stamped by the WS dispatcher in `server/index.js` on non-silent commands/dialogue/shop ops — client automation tags itself `silent:true` and never counts). See [plugins/idle-logoff/README.md](../plugins/idle-logoff/README.md) |
| **gps** | Resolve a location by (partial) name and plot a route to it | `gps run walk` | SIFT over `getAllZones()` (ambiguous picks replay via the `gps.navigate` Action); BFS route via engine `findPath` (pathfinding.js). Sends a `gps_route` response carrying the zone-id path, consumed client-side by `setGpsRoute` in `client/game/js/panels/minimap.js` — feeds the existing route-trace overlay shared by the sidebar minimap and full map popup; doesn't force the map open, but refreshes it (regional view) if already open |
| **ambient-life** | **City routine** — content-driven ambient vignettes that give opted-in street zones a daily rhythm (kids playing, delivery drones, buskers, construction, traffic, maintenance crews, food vendors, arguments, dogs barking). A `30s` tick scans zones with `flags.street_life` that hold a player, rolls per-zone, and fires a routine from the `ambient_routines` library matched on day-phase + `ambient_theme` + weather + optional zone allowlist. One `lines` entry = a one-shot; several = a paced scene (npc-banter shape). `loudness>0` bleeds the audible ones (drones/construction/traffic/dogs/arguments) into neighbours via `propagateSound`. **Two interactive:** a routine tagged `interactive` arms a short-lived per-zone opportunity + a clickable link — **street musician `tip`** (`[Tip ₵5]` → credits + small sanity) and **food cart `order`** (`[Buy a skewer ₵3]` → restores hunger); both resolve the live opportunity in the player's room (no SIFT). Layer over the engine ambient pool, not a replacement. Owns `ambient_routines` (content). Seed: `scripts/seed-ambient-life.mjs` (flags street zones + library). See [plugins/ambient-life/README.md](../plugins/ambient-life/README.md) | `tip order` | `30s` tick; reads `getEnvironmentState` (phase/weather), `world`/`getZonePlayers`, `propagateSound`/`sendToZone`, `adjustCredits` |
| **surveillance** | SPECTER — player spy networks + witnessed-crime wanted system (0–5 stars, police tiers up to Arbiters, evidence + bounties) | `plant retrieve sweep feed hub hubclose record clip clips wipe collect replay smash hijack pilot submit wanted bribe scrub purge devices destruct cast crush` (+ silent resolves `hijackresolve apprehendresolve`) | USE specialized actions on `spy_deck`/`security_console`/`datachip` tags; also registers the `WANTED_RAISE` Action (cross-plugin seam so jail can re-heat a jailbreaker). See [systems-surveillance.md](systems-surveillance.md) |
| **jail** | Get downed while **WANTED** (or live-arrested via the `ARREST` action) and the cops jail you instead of the vat: respawn in Precinct 9 Holding, serve **1 base-min/star × penalty multiplier** (default 6, game-clock scaled), cash seized + gear confiscated (concealment minigame can beat the scan), then a guard walks you out to the Lobby and returns your legal property + refund (weapons/drugs/hacking decks are kept in a shared 50-item evidence locker, purged after 3 days). The cell door is **police-only** (`canHack:false`) and genuinely **locked** — booking engages the hololock, the guard disengages it at release (and leaves it open until the next booking) — so any other way out is a **jailbreak** (heat returns via `WANTED_RAISE`, held gear forfeited). `sentence` (alias `time`) or `read <charge sheet>` prints your detention record: charge, stars, time remaining, held property. | `conceal concealresolve sentence time` + `read` on `charge_sheet` | Hook `player.respawnZone` (engine `handlePlayerDeath` seam — diverts respawn + skips the lootable corpse while inventory is intact so it can confiscate). Consumes `zone.entered` (any non-guard exit = escape). Owns `jail_prisoners` + `police_evidence` tables; hourly evidence purge; reschedules timers on boot. See [systems-jail.md](systems-jail.md) |
| **vendor-safe** | Hackable vendor safes storing accumulated sale credits | `hack`, `safecrackresolve` | Crack the combination via the **VAULT CRACK** rotary-dial minigame to drain them (winning is authoritative; payout re-read server-side on `safecrackresolve`, failure → 5-min lockout). If the safe's owner NPC is in the zone at arm time they catch you in the act — freak out to the room and report it (forced-witness `hacking` stars via surveillance's `vendor.safeHackWitnessed` listener). Getting caught, or successfully draining the safe, makes that vendor **hold a grudge** (engine `vendor-grudge.js`) and refuse to trade with you for 7 in-game days. Falls through (`undefined`) when no safe is in the zone so the doors plugin's `hack` (hackable door locks) can claim the verb |
| **hackrig** | **Practice lock rigs** — the Hacking on-ramp. Furniture flagged `hack_rig` is a dead lock panel with nothing behind it: same Circuit Breach minigame and same deck requirement as a live target, but **no credits, no crime, no shock**. **It retires itself:** every hack target now reports the TRUE margin (`marginOf` in [hack-gear.js](../server/engine/hack-gear.js)) to ip.js's `chance = base/(1+|margin|×scale)`, so a rig is just the *easiest* target rather than a special case. A difficulty-2 rig pays ~100% at Hacking 2, ~14% at 5, ~8% at 8 — crossing under a difficulty-5 ATM around Hacking 3–4, at which point it warns you in as many words (`OUTGROWN_MARGIN`) that a real lock teaches more. No cap or escalating difficulty needed — the shared curve evicts you, and keeps evicting you up the ladder. The grind's only cost is hardware — a failed attempt burns deck condition exactly as a live failure does (`server/engine/hack-gear.js`), which is the entire reason it isn't free XP. Default difficulty 2 (`flags.hack_difficulty` overrides), 20s pacing cooldown, all state in-memory. Falls through (`undefined`) when no rig is in the zone so vendor safes / shop vaults / hololock doors still claim `hack`. First rig is on Grady's wall in Two-Cell Supply. See [plugins/hackrig/README.md](../plugins/hackrig/README.md) | `hack`, `hackrigresolve` | No ticks, no tables, no DB writes of its own |
| **instrument** | **Playable musical instruments.** Furniture flagged `flags.instrument` (a voice: `piano`/`rhodes`/`musicbox`/`pluck`/`organ`) is sat down at with `play` and then played **key by key on the player's own keyboard**, through a docked two-octave tracker-layout keyboard panel; every note is heard by everyone else in the zone. **A note is not a command** — noodling is 8–12 inputs/sec, so notes ride a thin `instrument_note` ws route in `server/index.js` that only `emit`s (the `tv_watch` shape), rather than running the dispatch pipeline and printing a log line each. The server relays semantics only (`{voice, note, velocity}`, ~40 B) and each ear rebuilds the voice locally from the `INSTRUMENTS` table in `client/shared/procedural-sfx.js` — which is also what lets the player's **own** note sound client-side the instant the key goes down, since a round trip between pressing a key and hearing it is the difference between an instrument and a website. **No skill roll and no fumbles, deliberately** — the skill is the player's own hands. Authoritative state is who is seated, that they're still in the room, and a 14/sec **token-bucket** limit (a bucket, not an interval, so a four-note chord isn't thinned to one) — all in memory, no table, no tick, no query on the note path. `play c4 e4 g4` is the written route for the textgames rung and for macros. First instrument: the black upright in Bishop's Blend. See [systems-instruments.md](systems-instruments.md) | `play` | Consumes `instrument.note`, `zone.entered`, `posture.changed`, `player.stop`, `player.logout`; declaration-only specialized action on `requiredFlag: instrument` so examine advertises PLAY |
| **cosmetic-machine** | MORPHEX 9000 BioSculpt terminal | `morphex makeover biosculpt` | Self-gated `use` specialized action (machine names); engine furniture router opens the panel via the `cosmetic.open` Action |
| **amp** | AMP cassette economy — the personal-music player's server library ships **hidden**; a player only sees a track after feeding the deck its physical cassette. Cassettes are ordinary tradeable items (tag `amp_cassette`, `tags.song_id` → an `audio_songs` row): sellable to vendors, giveable, lootable — until inserted, which **destroys the tape** and permanently unlocks the track for that player. Also wires a **generic NPC gift trade** (`npcs.flags.gift_trade` = `{give_item, reward_item, once?, accept_message?, already_message?}`) — e.g. Nyra Voss trades a cigarette for the Psychotech cassette. Nothing hardcoded to a specific NPC/item. | `insert` | `insert <cassette>` consumes the tape → appends its song id to the persistent `amp_unlocks` player flag (JSON array) → pushes `amp_unlock` to the open AMP. Consumes `npc.gift` (fired by the give command when the target is an NPC — a **new engine seam** in `cmdGive`) to run the trade, and `player.login` to push the full `amp_unlocks` set to the client. Client gates the AMP library on that set (`client/game/js/panels/musicplayer.js`). Content: `scripts/add-amp-cassettes.js`. Emits `amp.unlocked` |
| **burglary** | Intrusion alarm — decides whether breaking into an *occupied* home earns burglary stars. A resident NPC must **hear** the break-in, then survive a ~10s panic cop-call (or flee the unit) before `burglary.reported` fires (→ surveillance charges it, forced witness). No verbs. Consumes `breakin.attempt` (doors hack/bash), `player.spoke` (say/yell noise), `player.attacked`/`npc.attacked` (combat noise + a hit resident flees), `npc.killed` (cancel). Detection: awake resident 100% on pick; asleep **20%/5s** while picking, **3%/5s** once inside — bypassed instantly by any intruder noise. Drives the NPC directly, suspending its AI graph via the engine `_ai.alarm` guard. **Also gates safe-sleep:** while a break-in is underway at a unit (intruder present), the owner can't raise the quantum forcefield — via `sleep` *or* by disconnecting — through the `forcefield.gate` hook (fired by `apartments.js activateForcefield`); so the burgled player sleeps exposed. (The reverse already holds: a door on an already-forcefielded unit refuses the hack.) 5s tick; all state in-memory. See [systems-jail.md](systems-jail.md) | — | `forcefield.gate` hook + 5s tick + event consumers; emits `burglary.reported`; depends on **surveillance** |

| **flight** | **Full flight system** (Phases A–D). Charter/buy/board aircraft at six airfields; fly the `map_world` tile grid with a continuous 60fps client sim the server reconciles (takeoff and landing are **flown, not commanded**); a **targeting** deck survives for the gun pass. Phase B hazards (stall/fire/weather/bird + `recover`/`extinguish`/`eject`), no-fly airspace (→ WANTED_RAISE), cargo/passenger + smuggling **contracts** (weight & balance), ground **AA + air combat** (arm/strafe → wreck), **hangars**, wreck **salvage/rebuild**, and deep **tuning**. Multi-file: `state.js` (shared registry/overlay/HUD/effStats/crash) + index/hazards/combat/contracts/hangars/acquisition. | **Board/crew:** `embark`(primary)`/board disembark deplane window takecontrols/controls handoff standdown` · **Fly:** `startup shutdown throttle heading`(cardinal or 247°) `climb dive`/`descend` `level turn flaps gear status takeoff land landat taxi hover divert nav circle orbit airhome flightwaypoint refuel preflight spot scan chart squawk flight/fs spray loadhopper` · **Hazard/escape:** `extinguish eject bail flares` (no `recover` — a stall is flown out of) · **Combat:** `arm safe evade strafe fire bomb airfire airlock airunlock` · **Cargo/contracts:** `contracts jobs accept manifest loadcargo jettison customs freightlicense cargolicense raws`(NPC-counter-gated) · **Acquire:** `buy rent charter charterinfo charterbook cancel cancelrental flushairborne`/`groundall` `checkride testfly showroom fleet carousel view sell` · **Hangar/tune:** `hangar hangaract repair salvage rebuild tune tuneset installkit loadout paintset scheme modify/customize`(owner-only) · **Context routers:** `examine look` · **Silent client resolves:** `strafresolve flightsync flightevent hopperbay` | The aircraft is a **first-class object owning its occupant set** (no runtime cabin zone; `player.aircraftId`/`seat` + synthesized `cockpit_update` HUD). Sky = **computed overlay** over `grid_x/grid_y`. Move gate (can't walk aboard) + input matcher (airborne cardinals set heading). **Verb-collision routers (manifest `after`, delegate by context):** `embark`/`board`/`disembark`→swimming's vessel actions when no aircraft is in play, `board`→gametable, `refuel`→generator, `buy`→commerce, `eject`/`tune`→broadcast, `repair`→engine builtin. Owns `aircraft`/`aircraft_types`/`flight_contracts`/`aa_sites`/`hangars`; airfields via `zones.flags.airfield_id`. Reuses Piloting/Fabrication/Chemistry skills, `WANTED_RAISE`, `handlePlayerDeath`. **`spray` lands on people, not just prose:** a low pass calls `applyTopical` per player in the tile below (see **topical**), so a hopper of water soaks and cools them — flight decides neither what the fluid does nor whether it may touch that person, and a flavour-only duster (hopper cap 0) stays flavour-only. Client: `client/game/js/panels/cockpit.js` (continuous sim + targeting deck + HUD). See [systems-flight.md](systems-flight.md) |

| **aa-sites** | AA emplacements **on foot** — the two-part installation (exposed deck + bunker) behind flight's ground AA: a `zone.describeRoom` panel on each `aa_sites` deck tile (battery name, faction, live status MANNED / ● FIRING / OFF-LINE UNDER REPAIR / cold ruin), a throttled room broadcast when the guns open up, and the repair loop — a strafed battery (`active=0`) is brought back online by its sheltered bunker engineer (`flags.aa_engineer`) unless that engineer is dead | — | `zone.describeRoom` hook; consumes `flight.aaFired`/`flight.aaSilenced`, emits `flight.aaRepaired` |
| **corps** | Corporations (player orgs): found/join a corp, custom ranks, a shared atomic treasury, a private corp channel, and a claimable HQ. NPC factions share the unified `orgs` table (the old `factions` table was folded in). Owns `orgs` + `zone_control` + `org_ventures` + `org_rackets`. **Protection rackets** (`rackets.js`): a corp that CONTROLS a zone can `shakedown` the shops in it for a cut of every sale — the cut is scaled by a `fear` value that **decays on a half-life**, so the income dies unless somebody keeps coming back. The take is debited from `npcs.vendor_credits` (a transfer, not a faucet) | `corp org shakedown` | See [systems-corps.md](systems-corps.md) for the design; the Tablet's Corporation app renders `buildConsolePayload()`. **`lean` is NOT this plugin's** — it belongs to `interactions` (furniture), which is why the racket verb is `shakedown` |
| **bartender** | Bartender-personality NPCs live behind the bar: a ~30s tick reads the live room and reacts in character — new players get clocked and dripped survival tips, a running poker hand gets rail commentary, the TV gets commented on by what's airing | — | 30s tick; registers `BARTENDER_ADVICE`/`BARTENDER_TV`/`BARTENDER_POKER` dialogue actions |
| **dealing** | Player-to-player drug dealing — `peddle <drug> to <player> [for <price>]` opens a priced offer (fair-value ±25% clamp), atomic pay + hand-off at accept, 60s TTL; emits `item.given` so surveillance witness-rolls `drug_dealing` | `peddle acceptdeal declinedeal canceldeal` | — |
| **district-ambience** | Per-district sensory leitmotif lines (smell/sound-texture/air) on the ambient tick, outdoor zones only; the lines are authored content (`content/districts/`), loaded by engine `districts.js` at boot | — | answers `zone.describeAmbient` |
| **unrest** | **The faction-conflict ledger** ([proposals/unrest.md](proposals/unrest.md)) — grip/heat/pressure per city block, so the tension between the orders is a thing the world carries rather than a line in the CODEX. **Phase 1a: state only.** No verbs, no hooks, nothing a player can perceive; 1b is the first step that says anything out loud | — | ⚠ **No player-facing readout, ever** — a number makes the sim a dashboard to optimise and the flavour dies; every scalar is visible at `/dev` and none crosses into `client/game/`. The cell is a **12×12 block derived from `grid_x`/`grid_y`** (`blocks.js`), never an authored district — that dependency is what made the first draft wait on [district-repair](proposals/district-repair.md), and nothing downstream knows what a cell *is*, so districts stay adoptable by swapping one key function. ⚠ Interiors resolve through `world_exit_zone` because grid 0,0 is an unset column, never a tile. **No table**: the ledger is one versioned `world_flags` blob (`unrest_ledger`), RAM-authoritative, decayed on READ rather than on a tick, write-behind on `5m`; the `30m` forcing tick is idle-gated. Roles are authored on `orgs.flags.role` and never a switch statement — ⚠ insurgency is *writes heat AND reads grip*, so the Wildblood (who read a clock) are a driver into the ledger rather than part of its cycle |
| **elevator** | Elevator car — a `flags.elevator` room whose `flags.elevator_floors` maps display floors to destination zones; describeRoom panel shows floor buttons. A ride moves nobody: the car settles with its doors open on the chosen floor and **`out` is the step onto it** (matcher-owned in a car, so it beats the authored `out` exit; the panel's open-doors `exit-link` is also what lights `out` on the d-pad) | `floor` | input matchers `\d+`, `up\|down`, `out\|exit` (car-only) |
| **residency** | Residents-only rooms: a `flags.residents_only: "<building>"` tile is walled off by a move gate unless the player holds a unit in that building (engine `isResidentOf`, read off the in-memory apartments cache). Shipped for the Solenne's private roof helipad | — | move gate `residency:residents-only`; the elevator runs the gate chain so a gated FLOOR is refused at the panel |
| **storefront** | **Player-owned shops.** A `flags.is_storefront` zone is a vacant retail unit for sale on authored terms (`shop_price`/`shop_term`/`shop_upkeep`): `buyshop` takes on the mortgage, `renameshop` puts your sign over the door (written to the **deed**, never the zone — a zone is content), `stock <item> for <price>` moves an inventory row onto the display, and anyone in the room buys it **whether or not the owner is online**. Instalments bill on `environment.dayRollover` every 7 game-days, drafted **till → bank → pocket**; clearing the term buys it outright and drops to upkeep, two consecutive misses repossess it *and* seize the stock. Takings sit in a `shop_vault` furniture piece anyone can VAULT CRACK. Deeds live in the player-class `storefronts` table (never exported, same law as `apartments`); listed goods stay as real `player_inventory` rows re-owned to `_shopstock_<zoneId>`, so condition/`custom_data` survive the counter and the shelf can't be `take`n. **Risk:** `shutters` works a real `lock:shopshutter` door (registered via the engine `registerLockType` seam, so lock/hack/bash/burglary apply unchanged; the deed holds durable state and re-applies it at boot like `apartments.is_locked`, and a vacant unit never ships sealed). `pocket` lifts stock off the display marked `custom_data.shop_unpaid` — settled by `buyware`, or charged as `shoplifting` on the way out under the normal witness law, with the proprietor **always** notified because it's a player's property. **Staff:** `hire clerk|guard` writes a `storefront_staff` row and room presence, deliberately **not an `npcs` row** (hiring is a player action; `npcs` is content-class, so a hired NPC would export into git — regress asserts the npcs count is unchanged). What the wage buys is odds: a lift or crack in front of staff emits `storefront.staffWitnessed` → surveillance charges it as a **forced witness**, the same convention as `vendor.safeHackWitnessed`. Unpaid staff walk before the lender forecloses. **Offline income:** a 5-min footfall tick sells sanely-priced stock (≤1.8× base value) to passers-by, and `buyorder`/`supply` are standing buy offers funded from the till. Ships 4 units across Coldwater. See [plugins/storefront/README.md](../plugins/storefront/README.md) | `deed buyshop renameshop sellshop stock unstock wares buyware pocket till tillcrackresolve shutters hire sack staff buyorder buyorders supply` | answers `zone.describeRoom`; `hack` is tag-gated on `shop_vault` and falls through (`undefined`) so vendor safes/hololock doors still claim it. **commerce `buy` delegates here** via the `storefront.buy_by_name` Action when no vendor NPC is in the room |
| **deaddrop** | **The finding half of a dead drop** — a container in a public room one player stocks and another empties without the two ever meeting ([proposals/dead-drops.md](proposals/dead-drops.md)). The PLACING half had shipped long ago and the two had never met: a census on 2026-08-30 found 57 furniture rows carrying `flags.container` and 58 carrying `flags.concealed`, and **not one row was both** — a dead drop is exactly that intersection and it had no members. So this is one `search.provider` contributor and nothing else. ⚠ **`flags.dead_drop` is a separate opt-in and must never be derived from `concealed`** — 53 of those 58 concealed rows are planted security devices, so a roll keyed on `concealed` would turn `search` into a generic counter-surveillance sweep and end SPECTER's plant-a-camera game, where beating a planted camera is supposed to mean knowing where to look. Three rules. **The provider never creates a cache** — one that springs into existence on a good roll is a faucet limited only by walking pace, the specific failure the plugin exists to refuse; it reports a row somebody already placed, the same relationship `strays` has to Cathode. **Finding is not opening** — a hit reveals the CACHE, never the contents (the item comes out through `container`'s own `open`, because it is a thing somebody paid for), and a paranoid dropper adds a `conceal_hides` row so `concealment` supplies a keypad unmodified: the chem-lab shape reused. And **the swept window is per cache per player, not per zone** — `search`'s own cooldown is per zone, so on a district grid a failed roll is re-rollable by stepping one tile out and back, which converts `STRANGER_BAR` (margin ≥ 12) from a wall into a wait; it is stamped on a miss as well as a hit for the same reason. RAM-authoritative, with `deaddrop_known_<cacheId>` in `player_flags` as the one durable piece, because being TOLD where a drop is has to survive a logout. There is deliberately no verb that makes you a knower — `tellPlayerAboutCache()` is exported for a quest or an NPC to call. ⚠ `object_type: 'container'` is NOT `flags.container`, and `open` gates on the flag: a cache authored by object type is found and then cannot be opened, which reads as a bug in `search`. Live caches: the Embassy washroom cistern, a dead mailbox in the Yards tenement lobby, and the hollow waiting bench in the Marquee precinct lobby. See [plugins/deaddrop/README.md](../plugins/deaddrop/README.md) | — (hooks only) | `search.provider` hook; emits `deaddrop.found`. Phases 2–4 (disturbed drops, player-placed caches, NPC couriers) are still design |
| **concealment** | **Furniture that hides furniture behind a passcode.** A disguise piece (`flags.conceal_hides` = the hidden row's id, `flags.conceal_code`, `flags.conceal_brand`) stands in the room looking innocent while the piece it names carries `flags.concealed` — the engine's **existing** room-description filter (`commands/describe.js`, the same one a planted spy camera uses), which is why this needed no new visibility seam. `keypad` pops a private client overlay (`panels/keypad.js`); the right code flips the flag through the `updateFurniture` funnel and the disguise pivots on a turntable. **The code is private, the reveal is public** — the digits go out via `sendCmdSilent` so they never reach the log, while the room watches the hidden piece roll out (a witness learns what you are, but still can't open it). A wrong code is mute on purpose. Live: the Cachet Vantage 900 cabinet in the drum basement hides `furn_chem_lab`, and **synthesis refuses a concealed lab**, so sealed means no cooking. See [plugins/concealment/README.md](../plugins/concealment/README.md) | `keypad concealcode concealsetcode` | `furniture.describe` hook; `keypad` specialized action gated on `requiredFlag: conceal_hides` |
| **generator** | Portable fuel generators: deploy, refuel from a fuel can, connect to a building junction box to back-feed power during an outage | `generator gen connect plug refuel` | flight wins `refuel` collisions by context |
| **gossip** | Global rumor pool: NPCs recall real events; players ask, spread, and follow leads | `gossip spread rumor` | dev-panel Gossip Pool tab |
| **insurance** | Halcyon Assurance — per-aircraft hull insurance: buy a fixed-term policy (`insure`), a covered crash files a claim collected at the claims desk for a partial payout; paid claims surcharge future premiums | `insure insurebind claim policies policy` | consumes `flight.crashed` |
| **intoxication** | Alcohol drunkenness — decaying BAC meter fed by `flags.alcoholic` drinks: slurred speech, wobbly movement, blackouts; coffee sobers | — | tick + drug hooks |
| **gatewarn** | One-time border briefing: the first time a player steps onto a tile with `flags.gate_warning`, the gate guards deliver that authored warning (dangers of leaving the city + which regions lie past the Curtain), gated by a per-player `gate_warned:<zone>` marker | — | consumes `zone.entered` |
| **tv-reactions** | NPCs answer the screen. Subscribes to `broadcast.message` (which the broadcast tick has always emitted per-room, and which nothing consumed until now) and occasionally has one idle NPC remark on the beat that just aired, keyed on the show's `playback_mode` so a ball game gets a groan and a sermon does not. **Owns the WHEN, and the restraint is the design**: one reaction per zone per 150 s, then only 18 % of the time, never on a studio floor (`onStage` — the cast are *saying* those lines), never without a player in the room (a spy cam is somebody watching the ROOM), and never within 25 s of the speaker's last line. Eligibility is npc-banter's own `eligibleNpcs()`, not a second copy, so sleepers and on-shift staff are out for the same reasons they never banter; the shared `_ai.lastSay` is also what stops this talking over a running banter scene. The throttle slot is claimed BEFORE the reply delay, or several beats landing together each queue a reaction and the room answers the television in chorus. See [plugins/tv-reactions/README.md](../plugins/tv-reactions/README.md) | — | consumes `broadcast.message` |
| **lore** | First-visit tone-setting lore: authored `flags.intro_lore` blocks woven into the room on a player's first look, gated by a per-player `lore_seen:<zone>` marker | `lorereset resetlore` (staff) · `lorealways` (per-player Extra Lore preference; the client re-asserts it each login from a LOCAL setting) | — |
| **posters** | Portable hero posters — `takedown` peels one off the wall (furniture → item), `putup` hangs it; Kiyo/Cyd seam easter egg | `takedown take putup` | — |
| **prologue** | The pre-world tutorial: new souls spawn in The Inbetween and walk a one-way corridor (chargen → first IP + holocaster → welcome broadcast) before the Coldwater clone vat; move gates hard-gate the narrative doors. **There is no tablet in the corridor** — `plugins/tablet/index.js` refuses `tablet`/`tabletnav` in any zone carrying `flags.prologue`, and the vat issues the device (`tablet_access` / `tablet_offer` pushes → a bobbing chip in the smart bar that opens the tablet walkthrough, or times out after 25 s). The clone-vat poster nudge then waits on the client's **`tabletdone`** echo (with a server backstop) instead of a timer. **Also owns the cold open** — a first login pushes `intro_cinematic` and holds ALL arrival prose until the client echoes `introdone` (62 s fallback), so the 30-second opening sequence never plays over the game. `intro` replays it; `tutorial` replays the interface tour, and **`tutorial tablet`** replays the short tablet walkthrough (which otherwise fires itself the first time a tablet home screen is drawn — client-side, `tabletTourSeen`). The interface tour is **layout-branched**: `client/game/js/panels/tour.js` filters its steps on `html[data-density]`, so the phone gets the collapsing room pane / D-pad / `≡ cmds` steps and the desktop gets the sidebar-rearrange step, and both end on a customisation + Settings beat. Grants CODEX Volume I when the welcome broadcast ends (filed to the record — there's no device to read it on until the vat) | `tutorial` `intro` `introdone` `tabletdone` | move gates. See [systems-codex.md](systems-codex.md) |
| **accolades** | **Accolades** — sardonic achievements framed as a surveillance file, not a trophy case (the prologue's "an algorithm notes that its very large number is, once again, correct" *is* this system). **Discovery-only** (no locked list, no total, no denominator — so entries can be added silently forever and players never see a seam), **fully private** (no broadcast, no viewing another player's file), **flat +1 XP per entry, forever, including the hardest** — the flatness is the joke. First entry fires on the prologue vat emergence ("So We Meat Again"). Named *accolades*, not *record* — `record` is owned by surveillance and the clash was constant | `accolades` `file` | Entries are DATA in `catalog.js` (`{key,title,line,on,test}`) — adding one is a single object literal, no wiring, since subscriptions derive from the catalog. Owns `player_achievements` (`player` class); PK `(player_id, entry_key)` **is** the XP safety story — re-trigger is `ON CONFLICT DO NOTHING` and `rowCount` (not the memory set) gates the grant, capping lifetime XP at one per entry vs 100/stat point, so nothing is farmable. FK to `players` ON DELETE CASCADE so non-player actors (enemies, corpses, the regress fake player) are a clean no-op. **First and currently only caller of `grantXp`.** Triggers hang off hot events (`zone.entered` fires per move) so nothing awaits a DB read on a trigger path: unlock sets load once at login, counters are in-memory in `player_flags`-backed `rec_*` keys flushed on logout. Emits `accolade.unlocked`/`accolade.opened` — entries can key off being noticed (*Meta*, *Reading This*). Banner: `client/game/js/panels/accolades-banner.js` (corner stack, 5s hold then a 520ms opacity fade, click-dismiss, hover-pause, max 3, reuses the `.intro-lore` sheen on the title; sparks must stay a **pre-rendered sprite blitted with `drawImage`** on a corner-sized canvas at DPR≤1.5 with a 70-spark cap — per-spark `shadowBlur` froze the client at 3 simultaneous cards). Cue `accolade-unlock` in `sfx-catalog.js` (three glass bells C5→G5→F5 "reach & relax", chorused twins on the ringing note, shimmer partials timed to the spark trickle at 300/600/900ms); tablet app `plugins/tablet/accolades-app.js`. Audition with dev-tools `.testaccolade [n]` |
| **strippers** | Tip-driven strip club: dancers escalate with shared stage heat; per-player 24h VIP pass (1000₵ tipped) gated by the viplock door; MIS allowed in VIP only | `tip` | — |
| **vending** | Dispenser furniture — `vend` pulls a data-driven item (`flags.vends`) from a machine in the room | `vend` | — |
| **cards** | **Trading cards** — a frozen snapshot of somebody, in three subject types on one shell: a **player** who minted themselves at a `flags.card_mint` terminal (₵2,500, 7-day cooldown), and the **NPC**/**enemy** cards struck from world content when a series opens. Only players mint, and that asymmetry is the whole justification: the card system deliberately does **not** follow anybody around (tracking every player's kit for a collectible would be a hot-path cost), so you walk somewhere and hand it the moment — this body, this loadout, these numbers, this thing you just said. NPC/enemy rows *are* static content, so those strike unattended and never go stale. **The rule that shapes the code is budgets, not truncation**: every region has a hard character cap and `builder.js` emits ranked clauses and stops at the first that would cross — an omitted clause is invisible, a truncated one is a bug the player can see. Condition is **spoken** on the front ("split at the knuckles") and named as a band on the back. Rarity is common→legendary (player: loadout tier; NPC: role; enemy: **spawn scarcity**, the only rarity signal the codebase has), plus an admin-only **architect** rank at `pool_weight` 0 that never rolls and never packs. Packs come from `flags.vends_packs` machines (powered, like everything else) — ₵900, sleeve **size is rolled** (4/5/6/7 + a 1% nine-card mis-cut), every card but the last rolled against the pool and the last excluding Common, revealed **worst-to-best** so the hit lands last | `mint` `mintquote` `cards` `buypack` `sleeve` `openpack`/`tear` `scrap` | Owns `cards` + `card_holdings` (runtime/player class — never export; `strikeSeries()` rebuilds the content-derived half on any DB). Consumes `player.say` (the Overheard line) and `furniture.describe` — the machine lists as ordinary furniture and its lit product window opens on `examine`, dark and useless when unpowered. Dev panel **🎴 Cards** at `/cards`. No tick, no hot-path query — the pool is cached behind one writer |
| **party** | First-class consent-based player groups (leader + roster, invite→accept). Drives the engine `follow` primitive so a party travels together (and crosses the void as one cohort). RAM-only runtime state; emits `party.changed`; exposes `getPartyView`/`getIncomingInvites`. Rendered by the **Tablet Party app** (`tablet/party-app.js`, 👥) | `party` | Drives `follow`; consumes `player.logout` |
| **voidwalking** | Void-travel: **walk off the rim** of a **void-region** (keyed by `flags.region_id`) — a cardinal step from a boundary tile into a coordinate holding no tile at all (`isMapRim`, via the `movement.edge` hook); there is **no entry verb** — into a generated **braid** of **transient (non-DB) rooms** (shared trunk forking toward multiple regions, risk-for-loot `west` detours, live enemy encounters via `spawnEnemySync`) and cross it on foot. A **party shares one instance** (cohort read off the `follow` substrate); relog-safe via `crossing_*` player_flags; consumes `zone.entered` / `player.login` / `player.death`. **Ghost-traces**: `scrawl <≤4 chars>` notes + corpses left by prior crossers, keyed by `(void, window, room_salt)` and cached per window (`void_traces`). **Loot**: `loot` a void room (Scavenging-skill-gated 2d8−2d8, detours richer, once per room) for tiered salvage. Stands on the `registerTransientZone` substrate ([systems-overland-void-travel.md](systems-overland-void-travel.md)) | `march` (**the traversal verb** — a bounded auto-walk along the trail that stops when there is something to decide. A room is a TILE, so Coldwater to Terminus is 282 typed `south`es of the same decision you already made when you set out, with the 32 authored highlights scattered through them at a 5.5% roll. Three rules. ⚠ **It is the ordinary move and not a second one** — every step calls the engine's own `cmdMove('south')`, so move gates, `zone.entered`, the window refresh, the encounter roll, followers, stamina and the arrival teardown all come along without this file naming any of them; both existing void move gates (a live foe seals `south`, a pitch seals `south`) halt a march with no code written for it. ⚠ **It costs the same MINUTES**, which is the whole of the pacing decision: thirst runs on the wall clock (1/12min awake, `SWEAT_THIRST_PER_MIN = 2` in heat), so a march that fired all 282 moves in one tick would cost approximately no water and would delete the springs, the cisterns, the barrels, `camp`'s twelve-thirst price and the reason a cut's "save twenty tiles" is worth anything. One tile per 2s tick is roughly typing tempo, so the verb removes the KEYSTROKES and not the JOURNEY. ⚠ **And the halt list is shorter than the interesting list** — encounters land every ~22 tiles and features every ~18, so halting on both is ~28 stops rather than the ~13 that makes this worth having, and `KIND_WEIGHTS` puts MARKER at 34% of features, which is a hubcap shrine and a chair. Scenery is not a decision: markers go past in the pane, and water/respite/shelter/salvage/hazard stop you. ⚠ **And the FORK is checked by identity, never by its exits** — the obvious reading is that it stops a march for free because `planFor`'s trunk loop never writes it a `south`, and that is wrong: each limb hangs off the fork in ITS OWN direction and Coldwater's first destination heads south, so the fork does have a way on and a march trusting the exits would have walked the player onto a heading nobody chose. ⚠ A branch is read off the PLAN, never off the zone's exits — a limb's first room carries a lateral back to the fork, so "has a west exit" would stop a march at the head of every limb in the game. ⚠ Thirst halts are GATES you cross, not a level you are at, or the verb stops working exactly where you most need it. Ended by the unified `stop`, or by any command **the player typed** — the `silent` flag now reaches `player.command`, because the client fires `sendCmdSilent('tabletnav map …')` on every move and "any command" would have killed a march on its first tile for anybody with the tablet map open) `scrawl` `loot` `frontier` `camp` (**the risky rest site**, at a wayside camp or a `respite`/`shelter` highlight. Nothing out here heals you — the engine's only passive HP regen is `healOverTime`/`wellFedUntil` and neither fires in a waste room — so a respite site GRANTS a heal-over-time rather than the void suppressing one, which is the same answer from the other side with one fewer moving part. ⚠ Paid for in WATER, which is the whole design of it: recovery is a gamble you choose, never a given, and the gap has real temperature now so a rest on the hot leg to Terminus costs more in practice without anything saying so. ⚠ And it rolls for an ambush on the ROOM's own odds, deliberately NOT through `maybeEncounter` — that fires once per room for ever, which is right for walking into a place and wrong for choosing to stay in one, and would make a cleared room a free hotel. The roll happens BEFORE the heal, so being jumped means you got nothing for the water. ⚠ It is `camp` and not `rest` because `rest` is an ENGINE ALIAS for `sleep` — a fourth way a verb can be taken, resolved before plugin lookup, and invisible to a check of manifests, builtins and specialized actions) `flag` (**put your arm out at a wayside camp.** A truck can carry people (trucking's `ride`/`hop`), so the only missing half was a way to ask. ⚠ A beacon has a LIFETIME and is not a state you sit in — a permanent flag turns every camp into a taxi rank and a driver's alert into a list of them. ⚠ And it is camp-only, which is geometry rather than a gate: a wayside is the one place the walking route and the road are the same place, so it is the only ground a rig CAN stop on for you. ⚠ Beacons are matched to drivers by COORDINATE, never by zone — two people in the same gap are in different transient rooms because a crossing is instanced, so "the same place" out there can only ever be a position. RAM-only: a logout or a restart clears it, and none of it is worth a row) `ready` (the muster's ready-check — every member of the cohort readies before the crossing launches; `voidwalk` survives only as the muster's `cancel`/`say` channel) | Owns `movement.edge` hook + `void_traces` (runtime); reuses Scavenging skill; fogged Frontier map (Tablet `frontier-app.js` 🧭); depth-scaling + credits-on-death pending |
| **trucking** | **THE LONG HAUL** — freight hauling by road, city to city across the waste. **You must own a truck**: four models on a real ladder (the 1,300₵ Krell Barrow to the 16,500₵ Orlov Continental; maker + haulage model, deliberately not the aircraft's animal family), bought at a yard, stored per-depot in a `trucks` table on the aircraft pattern. Deck capacity and tank range come from the TYPE, so what you own decides which trade routes are open. A haul starts at a **depot** (`flags.truck_depot`, content-authored), drives real city streets to the map's edge, crosses the void on a generated highway, and rolls into a depot in the far region. **One rig, two legs**: the only difference between them is the CELL PROVIDER — `surfaceAt` in the city, `corridorAt` in the void — so `mapWindow` renders both identically and the physics never learns which it is on. The corridor synthesises **zone-shaped** cells (never render cells), so road auto-tiling, biome, buildings and fog come from the one existing derivation. **The drive IS the crossing**: `current_zone` stays the void room and a node boundary emits `zone.entered`, so voidwalking's encounters, ghost-traces, hard nodes and teardown fire untouched, and a breakdown finishes on foot for free. **Buildings are solid** — `groundObstructionAt` is the flight sim's CFIT geometry at ground level with a `z0` gate, so you drive UNDER awnings and canopies (a third of the model set has mass over a truck's head) and INTO walls. **Two ways to earn**: `haul` is wages (a fixed fee to move somebody else's box), `market` is enterprise (your own capital on your own guess, and the only place you can end a run poorer). Prices are a pure function of (commodity, region, game-day) — no table, no tick, no DB — with uniform ~0.78/1.28 regional multipliers so RETURN stays near-constant and CAPITAL is the ladder. Client sims at 60fps; the server derives the odometer from reported POSITION against its own geometry and clamps against elapsed wall-clock, never re-simulating. City zone changes are RAM-only, flushed once on park/arrive. Meets voidwalking only through named exports (`crossingChain`/`crossingDest`/`crossingInfo`/`voidGateOf`/`launchCrossing`). See [systems-trucking.md](systems-trucking.md) | `drive` `roadtest` (**the driving lesson** — flight's checkride, for trucks. A free school Drayman is conjured at the yard as an ordinary `trucks` row stamped `custom_data.roadtest`, mounted through `drive` itself so the lesson teaches no special case, and deleted the moment the ride ends (plus a sweep at boot). Five stages — key, out of the yard, ten tiles of road, back, park — each of them a DISTANCE FROM THE YARD, which is the one measurement both rungs already maintain, so the cab's telemetry handler and the text tick share one evaluator and there is no second law to drift. Instructions go to the LOG rather than to a card over the windscreen, for the same reason. It is also **the licence**: `drive` is gated on `truck_licensed`, the mirror of flight's `air_pilot_licensed`. ⚠ **Flight shipped gated and this verb did not**, so the same gate bolted on flat would meet every existing driver at their own yard and confiscate the rig they bought — `isLicensedDriver` answers that by making OWNING ONE the licence (you demonstrably drive), written through on first ask so the fleet converges with no one-shot script; admins are rated, and a ride in progress is exempt or the door is locked from the inside. No grade, deliberately: flight can fail a landing because a bad one is a crater, and the equivalent here is parking badly. Bending it is remembered and never punished, since that is what a school rig is for) `hitch` `unhitch` `stash` `unstash` `pickup` `dropoff` `galley` `lock` / `unlock` (the CAB door latches — ⚠ both are **routers that fall through** to the engine door commands unless you are behind a wheel and either said nothing after the verb or named the cab, because a plugin beats a builtin outright and claiming them naively would point every apartment door, shutter and cell in the game at a truck. Doors default OPEN, persist per truck in `custom_data`, and an unlatched rig stopped for three seconds beside a hitcher gets a passenger who let themselves in — always into the SEAT, never the trailer) (what is in the cab that you could eat — one query when the flap is opened, never on the push, and every row it prints is the ordinary `eat`/`drink` verb string) `revs` `boot` `cruise` `coast` `brake` `jake` (the text rung's half of the cab's keys — named around flight, which already owns `gear` and `throttle`) `park` `ride` / `hop` (**the passenger seat.** An aircraft has carried people since charter (`live.occupants`); a truck was single-occupancy, so two people crossing the void together had to walk it. `ride [driver]` climbs into a STOPPED rig in your room — stopped because the rig is client-simulated and reconciled four times a second, so boarding one mid-move would put a second player's `current_zone` under a stale position, and because it is the same condition a HIJACKER boards under (`hijack.js`, STOPPED_MPH). Two seats, and the seeded hitcher in the sleeper takes one. ⚠ `rig.passengers` is deliberately NOT `rig.rider` — the rider is a seeded FACT about a stretch of road read in eleven places including the weighbridge, where clearing it is the whole fugitive mechanic, and putting a player through code that expects a description string and a weight is the bug that was waiting there. Riders are carried by `carryPassengers` inside the two zone movers (`driveToZone`, `crossToNode`) and nowhere else, so nobody is left standing in a street the truck drove out of; set down through `dismountRig`, so parking, a tow, a recovery and a driver logging out all release them by one path. ⚠ `hop` works at ANY speed — refusing would make a passenger the only person in the game who can be held somewhere by another player — it just costs you. With two cabs in the room `ride` asks WHICH rather than guessing) `fix` (a roadside repair on a breakdown — buys distance, never condition, and now needs a `item_truck_spares` box AND Fabrication ≥ 12, spent on the ATTEMPT rather than the success) `tow` (the way out when the rig is finished: the bottom of the condition bar is a wall, not a steeper slope — below `TERMINAL_CONDITION` the next tile breaks it every time and `fix` refuses outright. Paid, a low-loader takes you and the truck home; UNPAID it is recovered and held against the fee, which is the existing `impound_fee` path `drive` already settles — so nobody is ever stranded and nothing new stores the debt) `route` (which limb of the fork this rig takes; set it in the yard or take the other one at the junction) `rig strip` (a roadside wreck, stripped for parts — the second of three supply lines for a failed component, and the only one that costs no credits. It gives up the parts a person can LIFT and never an engine, because a crate dropped in a transient void room is freight you have thrown away — the same rule that stops you dropping a trailer out there. A hulk is stripped ONCE, marked on the shared wreck itself, so the next driver past finds it picked over) `rig fit` / `rig unfit` (**twenty cosmetic fittings in seven slots** — ram plate, tusk bar, roof cage, halogen light bar, beacon, totem rack, cut stacks, flame tips, neon stack sleeves, riveted plate, jerry rack, saw-blade skirt, chrome runner, underglow, lifter halos, beltline neon, chain rack, spare pod, banner pole, three bonnet mascots. **None of it is mechanical** and none of it reaches `effTruckParams`; storage is an array of ids in the `custom_data` bag already written on every bench commit, and the wire cost is a `^ab.cd` suffix on the mesh `variant` string every renderer already threads, so a stranger sees your bull bar with no new field on any payload. One per slot, and once owned, taking it off and putting it back is free) `rig wash` (the hose at the depot bench — a truck accrues **grime** on distance, scaled by the surface under it and pushed back down by rain, and this is the only thing that takes it off. ⚠ It is a `rig` SUBCOMMAND and not the verb `wash`, which the **mis** plugin owns; a truck one would have silently shadowed a consent-gated verb at every sink in the game. Purely cosmetic — no condition, no component, no part, no roll) `cb` (the radio — a real channel other drivers are on. `cb <words>` transmits, `cb 21` tunes, `cb speaker` reads incoming traffic aloud, `cb on`/`off`; bare `cb` is a status line, and everything that is not one of those four control words is TREATED AS SPEECH, because a radio whose default action is a settings change would be absurd. Access is the rig itself — a listener is a rig in `rigs` with the set on, tuned to your channel, so mounting puts you on the air and dismounting takes you off with no membership state anywhere to fall out of step. RAM-only and never replayed, and channels are 1–40 with everybody starting on 19 so the CB convention does the discovery) `horn` / `honk` (the two trumpets on the roof, which were ornament until the verb existed — heard by the ROOM, not by you, which is the only reason it is a verb rather than a keypress; works behind the wheel and standing in a yard beside a parked rig) `haul` `market` `yard` (+ `market buy`/`market sell` — the vendor plugins already own `buy`/`sell`) `fuel` (which now fills as far as the driver can AFFORD rather than refusing a short tank, and is the same commit the cab's hold-to-pump handle sends) (`trucksync` / `truckevent` / `truckpump` are the client-sim telemetry + discrete-transition routes, mirroring flight's pair) | Owns a move gate (you cannot walk while driving), a `zone.describeRoom` hook that teaches its own verbs and lists parked rigs, the `vehicle.contacts` gather-hook answer that puts moving trucks in pilots' windows (and reads `aircraftNearCoord` for the reverse), and the `truck_depot` / `truck_fuel` / `weigh_station` / `loading_dock` zone flags. **Both display-mode axes**: the cab is `prefersTextMinigames` (delete it and you are STUCK — textdrive.js runs the same stepTruck server-side), the board/exchange is `prefersLoggedPanels` (ONE depot panel — fleet, dealer, board and exchange on four tabs — that AUTO-OPENS on walking into a yard and closes on leaving, exactly as the hangar bay does; it is a skin over the identical text). Dispatcher NPC Rennie Vasch at Kessler Street Yard; `stepTruck` + the `hauler` type live in `client/game/js/panels/flight-model.js` beside `stepHeli`; cab is `paintWindshield` at `height: 0` with `cls: 'truck'`; **An eight-speed box with a splitter** whose rpm is DERIVED from road speed x gear ratio (the throttle makes torque), so lugging, over-revving, stalling and engine braking fall out of one curve rather than being coded — shipped with the diesel voice, because you shift by ear. **The rig**: a trailer on the fifth wheel with ONE free variable, the articulation angle, so jackknife is emergent and reversing inverts it for the cost of a sign; weight applied where it physically belongs (rolling resistance is a constant deceleration and is NOT divided by mass, brakes and aero are forces that are, which is why a load lengthens the STOP rather than the roll); brake temperature and fade, so holding a gear down a grade is what keeps the pedal honest; and cargo that needs a trailer, which is what makes bobtail a real way to drive. The mirrors draw the trailer at its true angle — the only place it is visible. **The scale house**, which is the one idea here native to trucks rather than borrowed: it detects **WEIGHT, NOT CONTRABAND** — it compares the trailer against the manifest and against its plate rating and does not know what the difference is, only that there is one — so detection keys off DISCREPANCY (counter-play: haul less or declare more, both of which cost money), a legal overweight load fails the same scale as a hidden one, and it is skipped in a lawless region, which is what makes Coldwater→Reach the smuggling direction and the return the one where you sweat. Answered with `customs open` (eat the loss, no charge) / `bribe` (priced off the lie) / `bolt` (a chase and a charge, and an impound lot you buy the truck back out of). The verb stays FLIGHT's — trucking answers it through a `TRUCK_CUSTOMS` action, the same seam the checkpoint plugin uses to run a scan through smuggle without importing it. **Trailers are world objects** (a `trailers` table): drop one and it stays dropped with its load still on it, in a zone that will still exist tomorrow — never a transient void room — with one-per-truck enforced by a partial unique index rather than a code path, and the deck rating on the BOX rather than the tractor. **Hitchhikers** are seeded per corridor node (same road, same person, all week) rather than NPC rows, and the fugitive closes the design: the sleeper is fast and visible, the trailer is hidden and weighs eighty kilos the weighbridge can see. "Visible" is now literal: they stand on the shoulder in the cab view, an alert stays up while they are ahead of you, and a scale house runs a **cab check** — a SECOND law at the same gate that knows nothing about weight (the weighbridge must never learn to recognise a person, or "weight, not contraband" stops being true), finds a sleeper fugitive every time with no roll and no bribe, runs bobtail, and ignores an ordinary lift ([proposals/the-long-haul.md](proposals/the-long-haul.md)) |
| **weightbench** | The gym. Posture-based workouts on station-flagged furniture — `lift` a bench (Brawn), `spar` a rebound wall (Reflexes), `drill` a circuit (Endurance). Sets grant a point at a rising rep cost, paid straight out of Net XP. Stations are data in `plugins/weightbench/stations.js` | `lift`, `spar`, `drill` | — |
| **yacht** | The Echelon — an invite-only admin superyacht in Coldwater Basin. Owns the invite list, the `yachtlock` access lock, the `yacht:board` move gate, and a smite-on-entry backstop that erases any uninvited player who ends up aboard. Owns the **Helm chase view** ([systems-helm.md](systems-helm.md)) | `invite uninvite invites sail sailto helm dock stop` | Depends on **doors**; tag-gated `use` on `teleporter`; consumes `zone.entered`; owns `yacht_invites` |
| **fireworks** | Admin-triggered synchronized fireworks show over the clone facility — staggered burst descriptions (origin + fainter in nearby rooms), propagated booms, on-foot screen flashes, flight-sim 3D bursts, and a weather-FX sky glow | `fireworks` | — |
| **consort** | **Kept companions, for anybody** — no longer two authored women on one yacht. A consort is any NPC with `flags.consort` + `flags.devoted_to`, and *who they are* comes from `flags.consort_archetype`: a registry of **12 sub-personalities**, each carrying a full set of devotion/arousal/shy/worried/pour/absence/talk/entrance pools. **Nothing resolves a voice by NAME any more** — that indirection is what fixed the silent breakage when the name-uniqueness pass renamed Bijou→Jolie, and what makes randomly-named generated consorts possible. **Both sexes**: every line is written with pronoun tokens (`{they}/{them}/{their}/{themself}` + a verb-agreement table) resolved per consort; regress renders every pool for both sexes and fails on an unresolved token. Physicality is generated from an appearance **seed** (build/height/hair/eyes/skin/mouth/grooming/scent/voice/age + optional scars and chrome), so the whole look regenerates from one column. **Ordering** is the MIS-gated **B.L.I.S.S.** tablet app (*Bonded Live-In Intimacy Subscription Service*): a seeded six-slot register rerollable on a **10-minute cooldown**, showing every physical characteristic plus how the placement describes *themselves* (never the archetype key); placed into any **private space you hold** (apartment you control, premises you own, or a zone flagged `private_billet_owner`) for a significant daily retainer, tiered by archetype/build rarity and **discounted the longer they stay** (−1.5%/day, floored at 55%). Billing rides `environment.dayRollover` drafted bank→pocket; two consecutive misses and the Syndicate collects them. **Consorts are LIVE-ONLY NPCs** spawned from the `player_consorts` ledger into `world.npcs` — deliberately never `npcs` rows (same law as storefront staff), so a roster can never export as world content. **PAIRINGS** are the rare high end: two archetypes placed and released together at a premium, and the only consorts that run two-hander scenes with each other (Cyd's Roxy & Jolie are now simply the first pairing, resolved by archetype membership rather than by name). They also react to **how long you've been gone**, in two real-time bands. See [plugins/consort/README.md](../plugins/consort/README.md) | `beckon dismiss pour` (keeper-only) + name-prefixed direct address | `npc.talk` (opens their `dialogue_tree` for the keeper, deflects everyone else) + `furniture.describe` + `player.say` hooks + `15s` tick + `environment.dayRollover` billing  **`bliss`** is the register by typing (subcommands `place`/`release`/`reroll`/`arrangement`): the whole hire-and-place economy used to be three buttons in the B.L.I.S.S. tablet app, bound to no command anywhere, so a text player could never acquire a consort at all. MIS-gated **identically to the app** — an unopted player gets the literal `Unknown command`, never a refusal, because a refusal tells you there is something there to be refused. The retainer arithmetic moved to `hire.js arrangementEntries()` so the two front ends cannot quote different daily costs for the same people |

A plugin with no player verbs and no specialized actions integrates purely through **hooks**
(request/response into engine flows) or **ticks** (scheduler cadences).

## When a system spans engine and plugin

Some mechanics are split: a plugin owns the **state** (what the player typed sets it) and the engine
owns the **reactions** (loops, combat, movement that read it). Posture is the canonical example. When
that happens, the plugin and engine **must agree on the field name and shape** — a mismatch makes one
half silently dead. Document the contract in the relevant `docs/systems-*.md` and run the
[source-of-truth audit](audits/source-of-truth-audit.md) when in doubt.
