{
  "name": "augments",
  "version": "1.0.0",
  "description": "Installable cybernetics — the machine-path mirror of mutations (chosen, paid, slot-limited, removable), plus the Ascendant cortical-backup death loop.",
  "commands": ["augment", "augments", "backup", "assurance"],
  "hooks": ["player.respawnZone"],
  "events": {
    "consumes": ["player.login"]
  },
  "actions": {
    "registers": []
  },
  "dependencies": ["ideologies"],
  "dataSchema": ["augments", "player_augments", "player_backups"]
}
