# container

**Purpose** — the container domain. One specialized action, gated on one tag. A leaf plugin in the truest sense: this is what "a plugin can be seventeen lines" looks like.

## Specialized actions
- `open` — gated on the `container` tag.

## Commands
None.

## Hooks
None.
