# debug

**Purpose** — a testing aid. `.debug` toggles a client-side reveal of dice rolls the game normally hides. It starts with IP-award rolls, and is the seam to hang any other hidden roll off.

## Commands
- `.debug` — toggle the reveal.

## Events consumed
- `ip.roll`
