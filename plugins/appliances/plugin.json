{
  "name": "appliances",
  "version": "1.0.0",
  "description": "Generic `flags.plugged_in` gate for any powered furniture (power_draw_kw set) — `power on/off <name>` verb plus a shared 'looks broken' examine hook when unplugged. Absent flag = plugged in, for backward compatibility. Consumed by vending and preservation.",
  "hooks": ["furniture.describe"],
  "commands": ["power"]
}
